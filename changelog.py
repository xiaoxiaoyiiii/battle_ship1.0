# -*- coding: utf-8 -*-
"""更新公告（2026-09-19）——**玩家视角的"这次改了什么"**，唯一真源。

为什么单独一个文件（与 `quick_chat.py` / `ranks.py` / `leveling.py` 同一套路）：
  * `api.py` 要下发这份数据；若写进 `server.py`，`api.py` 就得反向依赖 `server.py`
    （而 `server.py` 是 `from api import app` 的，会成环）；
  * 前端**不许**写死任何一条公告 —— 两份实现必然漂移（本项目的老病根），
    所以 `game.js` 只从 `GET /api/changelog` 取、只负责渲染。

--------------------------------------------------------------------------------
写公告的规矩（**给下一个 AI / 维护者**，别写成技术文档）
--------------------------------------------------------------------------------
1. **玩家视角**：以「修复了…」「改了…」「新增了…」开头，一条一句话，**≤ 40 字**；
2. **不许出现技术词**：文件名（`game.js` / `server.py`）、下划线或驼峰标识符、反引号、
   括号里的英文、`API` / `SQL` / `socket` 这类词；
3. **不许出现账号参数**：等级 / 段位分数 / 解锁 / 特权 之类具体数字或开关名
   （与本仓库「提交说明只写改了什么、不写账号参数」同口径）；
4. **每批更新都在这里最上面加一条**（`date` 用本机时间 `YYYY-MM-DD HH:MM`）。
   漏加不会报错，但玩家就看不到这次改了什么 —— 这是本文件的**唯一用途**；
5. `date` **必须唯一**：它既是展示用的时间，也是前端"这条我看过没有"的标识
   （`localStorage` 存的就是它）。守卫见 `tests/test_changelog.py`。

⚠️ 顺序是「**最新的在最前**」，`view()` 不做排序 —— 排序是数据自己的责任，
   守卫测试会钉「严格倒序」，写的时候顺手放最上面即可。
"""

from typing import Any

# 每条：{'date': 'YYYY-MM-DD HH:MM', 'items': ['修复了…', '改了…']}
# 时间取自各批次的真实提交时间（见 `git log`），不是随手编的。
CHANGELOG: list[dict[str, Any]] = [
    {
        'date': '2026-09-21 23:10',
        'items': [
            '改了：「大师」难度现在会认真挑牌了，也会用探测、复活这些牌，明显更难打',
        ],
    },
    {
        'date': '2026-09-21 19:00',
        'items': [
            '新增了：人机对战多了个「大师」难度（强度还在调优中）',
        ],
    },
    {
        'date': '2026-09-21 14:20',
        'items': [
            '修复了：格子全打完但还剩攻击次数时，回合会卡住交不出去',
        ],
    },
    {
        'date': '2026-09-20 15:00',
        'items': [
            '新增了：普通魔法卡「滥竽充数」，补充战舰到满船数，大回合结束时收回',
        ],
    },
    {
        'date': '2026-09-20 14:30',
        'items': [
            '新增了：普通魔法卡「卧薪尝胆」，自己船数落后时给所有船加护盾',
            '新增了：普通魔法卡「守株待兔」，船带陷阱被击沉时对方牺牲两艘战舰',
            '新增了：普通魔法卡「亡羊补牢」，从弃牌区最新n张牌里挑1张回手牌',
        ],
    },
    {
        'date': '2026-09-20 14:00',
        'items': [
            '新增了：判定魔法卡「命运骰子」，摇骰子按点数结算不同效果',
        ],
    },
    {
        'date': '2026-09-20 02:40',
        'items': [
            '修复了：回光返照重摆棋盘后会重新猜拳定先手',
            '修复了：回光返照之后本回合打不出去的问题',
        ],
    },
    {
        'date': '2026-09-20 02:10',
        'items': [
            '修复了：神威区域恢复后，回来的战舰看不见',
            '修复了：回光返照和绝处逢生会让对手也重摆棋盘',
            '修复了：神之宣告跳过对方阶段时会多跳一个大回合',
            '修复了：重摆棋盘后极限增援、无暇圣心还在悄悄生效',
        ],
    },
    {
        'date': '2026-09-20 01:10',
        'items': [
            '新增了：反复刷分会被逐步限制，先是排位、再是匹配',
            '改了：老老实实玩的账号完全不受影响',
        ],
    },
    {
        'date': '2026-09-20 00:30',
        'items': [
            '改了：匹配时尽量不让你连续碰到同一个人',
            '新增了：同一台机器上的多个号互相碰不上',
        ],
    },
    {
        'date': '2026-09-20 00:05',
        'items': [
            '新增了：反刷分系统，异常对局不再能加排位分',
            '调整了：靠小号刷出来的排位分与战绩已经清掉',
            '修复了：少数对局能靠固定的摆船位置一路赢到底',
        ],
    },
    {
        'date': '2026-09-19 20:14',
        'items': [
            '新增了：更新公告（就是你现在看的这个）',
            '修复了：神之宣告在船太少时不该能打出去',
            '修复了：重置棋盘后神威、冻结的范围还在生效',
            '修复了：帮助面板会压住刚打开的弹窗',
        ],
    },
    {
        'date': '2026-09-19 17:08',
        'items': [
            '修复了：好友约战点了没反应、开不了局',
            '修复了：一方卡在一块空棋盘上等不到人',
            '修复了：解散房间后就没法再约战了',
            '修复了：同一个账号开两个页面也开不了局',
            '新增了：好友私聊，聊天记录会保留、每条都带时间、有未读提示',
        ],
    },
    {
        'date': '2026-09-19 15:50',
        'items': [
            '修复了：从大厅进过一局之后，大厅就打不开了',
            '修复了：几个弹窗会互相压住',
        ],
    },
    {
        'date': '2026-09-19 15:06',
        'items': [
            '新增了：好友功能（加好友、看谁在线、一键邀战）',
            '新增了：打完一局可以直接加刚才那个对手',
            '新增了：可以设置允不允许别人加你为好友',
        ],
    },
    {
        'date': '2026-09-18 21:43',
        'items': [
            '新增了：游戏大厅（在线玩家、房间列表、大厅聊天）',
            '修复了：连点创建房间会在大厅堆出一堆同名房',
        ],
    },
    {
        'date': '2026-09-18 18:18',
        'items': [
            '新增了：段位与排位赛',
            '新增了：段位可以解锁头像框和名片底色，高级的外观会动',
            '修复了：段位外观解锁了却看不到',
        ],
    },
    {
        'date': '2026-09-18 17:27',
        'items': [
            '改了：排位分的算法，现在按整局表现来算（不再只看输赢）',
            '新增了：帮助页里的段位说明',
        ],
    },
]


def view(limit: int = 10) -> dict:
    """给接口用的视图：截断 + **拷贝**（绝不把内部列表交给调用方）。

    ⚠️ 拷贝是必须的：`api.py` 拿去 `jsonify` 之后，只要有人往内部列表里 append，
    下一次响应就会带上别人的数据（本仓库"同一份数据被多处持有"栽过）。
    """
    try:
        n = int(limit)
    except (TypeError, ValueError):
        n = 10
    n = max(1, min(n, 20))          # 上限 20：公告是给人看的，不需要分页
    entries = [
        {'date': str(e.get('date') or ''), 'items': [str(x) for x in (e.get('items') or [])]}
        for e in CHANGELOG[:n]
    ]
    return {'latest': latest_date(), 'entries': entries}


def latest_date() -> str:
    """最新一条的时间（没有条目时返回空串）。

    前端拿它跟 `localStorage` 里存的"看过的最后时间"比：不一样就说明有新公告。
    """
    if not CHANGELOG:
        return ''
    return str(CHANGELOG[0].get('date') or '')
