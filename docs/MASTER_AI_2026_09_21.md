# 大师难度 AI 设计规格（2026-09-21）

> 目标：新增第 4 档人机难度「大师」。接近人类高手但**可以赢**——会读局势、会用几乎所有手牌、会在关键节点嘲讽。
> 本文是实施前的规格。**实施时以 grep 结果为准，本文行号只是线索。**

---

## 0. 一句话

现有 AI 是「从 9 张安全卡里挑最便宜的，然后闭眼随机开炮」。大师 AI 要变成
**「读局势 → 挑最该打的那张牌 → 自己回答这张牌需要的交互 → 按情报开炮 → 打完收尾」**。

---

## 1. 现状（已核实，作为基线）

| 事实 | 位置 |
|---|---|
| AI id = `'ai-' + room_id`，`room.is_ai_room=True`，`room.ranked=False` | server.py:701-708 |
| `AI_DIFFICULTIES = ('easy','normal','hard')`，未知值静默回落 `normal` | server.py:5243 / 704 |
| `_AI_SAFE_CARDS` 只有 **9 张** | server.py:5252-5255 |
| `_ai_choose_magic_card`＝过滤白名单后**按速阶升序取最小**，无局势评估 | server.py:5258-5276 |
| `_ai_maybe_play_magic`：easy 不出牌；其余每回合**最多一张** | server.py:5279-5297 |
| **hard 只比 normal 多一条能力**：连锁里用「失灵！」反制 | server.py:6415-6420 |
| 开炮是 **`random.choice` 均匀随机**，三档难度完全一样 | server.py:5334 |
| AI **从不读 `revealed_positions`** —— 有情报也不用 | 全文件无引用 |
| 唯一的「AI 自动解决交互」通道：`_request_ship_pick` 里 `random.choice(alive)` | server.py:9000-9001 |
| 区域/行列/格、挑牌、放置流程：**没有任何 AI 自动路径** | `_start_placement` 9423 只 emit 给施法者 sid |
| 运行期**没有**「AI 打不了就跳过」的机制；护栏只有白名单 + 测试 | tests/test_ai_magic.py:78-100 |

### 1.1 现状里的四个缺陷（本次一并处理）

1. **「余音绕梁」在白名单里但 100% 打空**：它要求 `current_phase=='preparation'`（9604），
   而 AI 先进战斗阶段才出牌（5310→5315）→ 每次被 `_refund_card_to_hand` 退回。
   且 `_ai_maybe_play_magic` 返回的 `True` 只表示「进了连锁」，不代表效果生效。
2. **「回光返照」让 AI 硬死锁**：`room.state='placing_ships'`（10999）且只通知施法者=AI，
   而 AI 没有摆船入口 → `enter_end_phase` / `end_turn` 全被拒 → 回合永远交不出去，真人零提示。
   「败者食尘」（9870）同构，只是可能靠真人先摆而自愈。
3. **「教皇旨意」对 AI 是自杀**：打出后 `attacks_remaining = 0`（9787），
   而 AI 完全没有「弃卡换攻击 +2」的代码。
4. **`_ai_turn_loop` 无上限、无「正在跑」标记**：40×0.3 + 20×0.5 + 20×0.5 ≈ 32s+，
   且 `_maybe_run_ai_turn` 有 4 个触发点（3067/3152/4847/5196），
   重摆棋盘后可能并发起两个 turn loop（未复现，但代码里无 guard）。

---

## 2. 非目标（明确不做）

- **不改 easy / normal / hard 的任何行为**。三档代码路径保持逐字不变。
- **不让 AI 作弊**。见 §4.1——这是硬约束，不是「尽量」。
- 不做多步搜索 / MCTS（信息不完全，收益配不上延迟与可测性代价）。
- 不改段位/统计口径（人机本来就不计分，保持现状）。

---

## 3. 架构

新增纯规则模块 **`ai_brain.py`**，与 `ranks.py` / `anticheat.py` / `match_guard.py` 同一路数：
**纯函数、无 I/O、无 socket、无全局状态**，因此可纯单测。

```
server.py  ──(仅 difficulty=='master')──▶  ai_brain.py
   │                                          │
   ├─ 出牌时机 / 回合循环（接线）              ├─ choose_card()      手牌打分
   ├─ _request_ship_pick 的 AI 分支            ├─ choose_attack()    开炮决策
   ├─ 区域/行列/格 目标                        ├─ resolve_ship_pick() 选船
   ├─ 挑牌（弃牌堆/牌堆/对方手牌/数值）        ├─ resolve_target()   选区域/行列/格
   ├─ 放置流程应答                             ├─ resolve_choice()   挑牌/数值
   └─ 嘲讽 emit                                ├─ resolve_placement() 新船放哪
                                               └─ choose_taunt()     关键节点台词
```

**为什么单独成模块**：本需求真正的风险是「AI 打出一张需要自己回答交互的牌 → 回合永久卡死」，
症状是**不报错、不掉线、只能下一局才发现**。这类 bug 只有纯函数单测能提前钉住；
把决策逻辑内联进 11,400 行的 `server.py` 等于把风险全推到线上。

`server.py` 的接线点一律写成 `if _master(room):` 一行分支，**默认路径完全不动**。

---

## 4. 决策函数规格

所有函数签名统一：

```python
def choose_attack(room, ai_id: str, rng: random.Random) -> tuple[int, int] | None
```

`rng` 注入而非用全局 `random` —— **为了可复现**（测试要能钉死具体招法）。
所有函数必须对残缺状态安全：拿不到必要信息时返回 `None`，由调用方兜底，**绝不抛异常**。

### 4.1 硬约束：AI 不许作弊

AI 代码跑在服务端，**理论上能看到 `room.players[opponent].ships`（对方船位）**。
一旦读了，AI 就变成全知，强度是假的，玩家也会立刻察觉。

**规章：`ai_brain.py` 只允许读「真人玩家在该座位上也能看到的信息」白名单：**

| 允许 | 不允许 |
|---|---|
| 自己的手牌 / 船 / 攻击次数 / 效果标记 | `opponent.ships` 的**位置与存活** |
| `room.players[me].revealed_positions`（自己探明的） | `opponent.magic_hand`（除「明智埋葬」按卡面**明示可见**时） |
| `opponent.remaining_ships`（界面上就显示） | `opponent.effect_flags` 里未公开的部分 |
| `room.magic_discard`（弃牌堆公开）、`magic_history` | `room.magic_deck` 的**顺序与内容** |
| `room.last_attack`、`game_logs`、场上已发生的公开事件 | `room.players[opponent].ships` 的任何遍历 |

**守卫测试（必须写）**：源码级断言 `ai_brain.py` 不出现 `opponent.ships` / `.magic_deck`
之类越权读取；再加一条行为测试——把对方船位改到不同位置、其余状态不变，
**`choose_attack` 的输出分布必须完全一致**。这条测试是「AI 不作弊」的唯一硬保证。

### 4.2 `choose_attack(room, ai_id, rng) -> (x, y) | None`

船是**单格船**（6 艘各占 1 格，6×6 棋盘），所以没有「船形推理」的余地，
情报就是全部优势。规则：

1. `known` = 自己已探明的敌船位置（`revealed_positions`）− 已轰过的格子。
   非空 → 从中挑一个（**必中**）。
2. 否则 → 在「未轰过的格子」里均匀挑。
3. 无候选 → `None`。

> 这一条单独就能明显提升强度：现状是连自己探明的位置都不打。

**现状对比**：`server.py:5334` 是 36 格均匀随机，`revealed_positions` 一次没读。

### 4.3 `resolve_ship_pick(room, ai_id, reason, rng) -> ship | None`

按**目的**分策略，替换现有的 `random.choice`：

| reason | 策略 |
|---|---|
| `demon_contract` / `divine_decree` / `dice_sacrifice` / `trap_sacrifice`（要牺牲） | 挑**信息价值最低**的船：优先挑「周围格已被对方轰过」的（对方已排除那片区域，等于没有额外信息） |
| `kraken_eye`（要被窥探） | 同上——挑泄露代价最小的，不是随机送一条 |
| `shield_choice`（仁王之盾，多选） | 优先血少/靠前的船，凑满上限 |
| `trap_setup`（守株待兔） | 挑**最可能先被打**的船（对方已探明区域附近的），让陷阱尽早触发 |

只从 `_alive_ships` 里选（沉船不能抵账，已有测试钉住：`test_alive_ship_selection.py:153-165`）。

### 4.4 `resolve_target(room, ai_id, card, rng) -> dict | None`

返回 `{'target_area': {...}}` / `{'target_line': {...}}` / `{'target_cells': [...]}` / `{'ship_indices': [...]}`，
形状必须与现有 `confirmMagicTarget` 接受的一致。

评分函数（对任意候选区域/线/连续段）：

```
score = 100 × (区域内自己已探明的敌船数)
      + 1   × (区域内未被轰过的格子数)     # 信息价值
      - 50  × (区域内自己的船)             # 神威等对己方有害的卡要反向
```

- **范围卡**（神威 / 冻结 / 探测雷达，3×3）：枚举全部 16 个位置取最高分。
- **行列卡**（轰炸）：6 行 6 列取最高分。
- **连续段**（硫磺火焰，6 格连选）：滑窗取最高分。
- **给自己船加盾/标记**（仁王之盾、守株待兔）：按 §4.3。
- 神威打**对方棋盘**时用上面的分；打**己方棋盘**时把「自己的船」权重取负。

### 4.5 `resolve_choice(room, ai_id, kind, options) -> any | None`

| kind | 卡 | 策略 |
|---|---|---|
| `wangyang_choice` | 亡羊补牢 | 从弃牌堆窗口里挑**价值最高**的那张（用 §4.7 的牌价值表） |
| `bury_choice` | 明智埋葬 | 候选含**对方手牌（卡面明示可见）**→ 优先埋掉对方**最能翻盘**的那张（按牌价值表取最大） |
| `taoyuan_choice` | 桃园结义 | 按牌面给的选项取价值最高者 |
| `lingqi_choice` | 灵气复苏 | 选数值 x：自己船多就保持自己船数，落后则在允许范围内取「对方船数 − 1」 |
| `pending_dice_discard` | 命运骰子 3 点 | 弃**价值最低**的一张 |

> 「明智埋葬能看对方手牌」是卡面明示的规则，不属于作弊。

### 4.6 `resolve_placement(room, ai_id, kind, candidates) -> (x, y) | None`

用于增援 / 死者苏生 / 滥竽充数 / 绝处逢生 / 神机妙算 re-deploy / 回光返照 / 败者食尘。

规则：**在合法候选里最大化「对方没轰过的邻格数」**——即尽量放在对方**已经轰过**的格子
（对方无法重复轰同一格，放在那里等于免挨打）。

⚠️ **实施前必须先核实每张卡对候选格的限制**（例如滥竽充数的卡面写「未被对方打过的空格」，
那就不能放已轰格）。**以各卡分支实际算出的 `candidates` 为准**，本函数只在给定候选里挑。

### 4.7 `choose_card(room, ai_id, rng) -> int | None`

返回手牌下标。分两步：

**(1) 牌价值表**：模块级常量 `CARD_BASE_VALUE`，**47 张可摸到的卡每张一个值**（不许留空）。
定标规则（不是拍脑袋，按这个推）：

```
进攻性（直接减少对方船数）  80-95   例：轰炸 90、神威！ 85、克苏鲁之眼 70
资源/手牌（换牌、抽牌）     55-75   例：无中生有 70、亡羊补牢 65
防守/续航（回血、加盾）     50-70   例：疗愈 65、仁王之盾 60、卧薪尝胆 55
条件性（需前置才能生效）    +10    在基础值上叠加；条件不成立时归零
重摆类（回光返照/败者食尘） 30-45   局面越差越加分（见下面的局势乘子）
```

完整数值表是**实现阶段的交付物**，本文只定标定规则——因为数值要靠真机对局调，
写死在规格里反而会误导。

**(2) 局势乘子** = 基础值 × 条件因子：

| 因子 | 说明 |
|---|---|
| 前置条件 | 「溅射 / 雷达子弹 / 越战越勇 / 饮血」要求 `room.last_attack` 刚命中或刚击沉 → **条件不成立时价值归零**（现状是白打退牌） |
| 阶段门禁 | 「余音绕梁 / Freezing！」只能在准备阶段 → 由调用方在准备阶段单独给一次出牌机会（见 §5） |
| 局势 | 落后时进攻卡加分、领先时防守卡加分；手牌少时「无中生有」加分 |
| 牌数预算 | 大师每回合上限 **3 张**（现状 1 张）。预算耗尽即停止出牌，避免倒光手牌 |
| 不可打 | `can_play_magic_card` 返回 False 的一律跳过 |

排序后取最高分且**严格大于阈值**的一张；没有就返回 `None`（宁可不出，也不白打）。

### 4.8 `choose_taunt(room, ai_id, event, rng) -> str | None`

- `event` 由调用方在关键节点传入，取值见 §6。
- 台词表做成**数据 + 纯函数**（照 `quick_chat.py` 的形状），每个 event 一组候选。
- **同一 event 每局最多触发一次**、**两次嘲讽间隔 ≥ 20 秒**（防刷屏）。
- 返回 `None` 表示这次不说话。

---

## 5. 回合循环改造

现状（`_ai_turn_loop` 5300-5366）：`进入战斗 → 出一张卡 → 等连锁 → 轰完 → 结束阶段 → 交回合`（固定脚本）。

大师改为**决策循环**（普通/困难继续走原脚本，一行不改）：

```
进入准备阶段（仅大师）
  └─ 若手牌里有「余音绕梁 / Freezing！」等仅准备阶段可打的牌 → 给一次出牌机会
进入战斗阶段
loop（最多 N 轮，N 有上限）:
  1. 若存在「等我回答的交互」（选船/目标/挑牌/放置）→ resolve_* 回答它，continue
  2. 若上一次攻击刚命中/刚击沉，且手牌里有「依赖刚命中」的卡且值得打
     → 立刻打出来，continue        # ★ 必须在下一炮之前，否则窗口就没了
  3. 若 attack 次数 > 0 → choose_attack 选一格开炮，continue
  4. 若出牌预算未耗尽，且 choose_card 认为有值得打的牌 → 打出来，continue
     # ↑ 攻击次数用尽后仍要走这一步：卧薪尝胆 / 疗愈 / 场地卡 / 抽牌
     #   都可能在没有攻击次数时依然值得打
  5. 否则 → break
结束阶段（沿用现有重试逻辑）
交回合（沿用现有重试逻辑）
```

⚠️ **顺序是本循环最容易写错的地方**：步骤 2 必须在步骤 3 **之前**。
若写成「先开炮、打完再考虑出牌」，那么每次命中后循环都会直接开下一炮，
「溅射 / 雷达子弹 / 越战越勇 / 饮血」的窗口永远被自己错过——这正是
「卡在卡池里、却永远用不对时机」的成因。

**为什么必须交错**：现在「先出一张卡、然后一路轰完」的结构下，
「溅射 / 雷达子弹 / 越战越勇 / 饮血」这些**依赖刚命中**的卡永远是废牌——
只有把出牌和开炮交错起来，它们才可能被用对时机。

### 5.1 防卡死（本设计的重点）

#### 5.1.0 现状的三个洞（已核实）

现有看门狗 `_tick_turn_timeouts`（server.py:7083）会让大师 AI 正好掉进去：

```python
7093:  if room.state != 'attacking': continue      # 回光返照把 state 改成 placing_ships → 不管
7095:  if room.is_ai_room: continue                # 「AI 自己会走完回合」→ 整类房间跳过
7114:  if (pending_placement or pending_shenji or _my_ship_picks(...)): continue   # 有待办就不催
```

**「AI 自己会走完回合」在 AI 只打 9 张无交互卡时成立；放开到 45 张时当场失效**，
而唯一的兜底还会主动跳过这些房间。所以不能只在现有看门狗里加东西。

#### 5.1.1 判据必须是「在等谁」，不是「多久没动」

⚠️ **反面设计（会毁掉真人对局）**：「房间 T 秒无状态变更就强制推进」——
这个判据**分不清「AI 卡住」和「真人正在长考」**。真人想 30 秒完全正常，
而现有代码在等待点选时**故意不催**（7114-7116 注释：「等待点选是玩家正在操作」）。
任何「全局无进展就推进」的规则都会推翻这个有意的设计，把真人正在思考的一手搅掉。

**正确判据**：

```
① 先算「这个房间现在在等谁」（responsible）：
     有待办（pending_ship_picks / pending_placement / pending_shenji）
         → responsible = 该待办的 owner
     否则
         → responsible = room.current_attacker

② 按责任方身份给**不同**额度：
     responsible 是 AI   → AI_STALL_SECONDS（默认 15s）→ 超时执行强制推进
     responsible 是真人 → **完全沿用现有 TURN_TIMEOUT_SECONDS（90s），一行不改**
```

要点：
- **真人的额度与语义一个字不动**：90 秒、每次有效操作重新计时、等待点选时不催，
  全部保留。真人想多久还是多久。
- **AI 用独立的更短额度**（新增 `AI_STALL_SECONDS`，与 `TURN_TIMEOUT_SECONDS` 分开，
  后者是给真人用的、可由环境变量配）。AI 正常 1-3 秒就动完，15 秒已非常宽裕；
  超过 15 秒只可能是卡住。
- 因此**强制推进只会在「AI 是责任方」时触发，永远不会碰真人的思考时间**。

#### 5.1.2 分层

| 层 | 做什么 |
|---|---|
| **L0** | 不卡：brain 回答所有交互（§4） |
| **L1** | AI 回合墙钟上限：`_ai_turn_loop` 加计时 + 轮次上限；超时走收尾 |
| **L2** | **`_force_advance_turn(room, why)`**：清掉阻塞项 + 推进回合，**大声记日志**。这是「直接跳过卡死」的原语 |
| **L3** | 不按 `state` 门禁：必须覆盖 `placing_ships`（自动摆船，复用 `_ai_place_ships`）与 `preparation` |
| **L4** | 责任方停滞检测（§5.1.1）：AI 责任方 15s → 强制推进 |
| **L5** | 连续 N 次强制推进仍失败 → 判和/作废对局并释放房间。**保证房间一定被释放** |

#### 5.1.3 逃生按钮：「卡住了？点这里试试」（**仅人机房**）

**定位**：这个按钮是**专为人机对战设计的**——大师 AI 放开 45 张卡后，
最可能卡住的是 AI 那一侧。**不做真人对局的救援**（真人对局的方案见 §5.1.4）。

**为什么在 AI 房里它是安全的**：AI 房间只有**一个真人**（就是请求者本人），
不存在「代另一个真人推进回合」的滥用口子。所以裁决可以很简单：

```
socket: force_continue {room_id, player_id}

  房间不是人机房        → 拒绝：「这个功能只用于人机对战」
  房间不是这个人的       → 拒绝
  责任方 == 请求者自己   → 清掉**他自己名下**的死待办，让他能继续
  责任方 == AI          → _force_advance_turn(room, 'user_requested')，推 AI 一把
  房间本身正常          → 什么都不改，返回「看起来一切正常」
```

**位置**：局内界面常驻一个小控件（底部 dock 的「日志 / 聊天」那一排），
文案 `卡住了？点这里试试`。**不做成弹窗**。
当服务端判定「责任方是 AI 且已停滞超过 15 秒」时，把这个控件**高亮**出来
（只高亮，不弹窗）。

**要点**：

- **正常状态下点它不改变任何对局状态**，所以可以放心让它常驻。
- 频率限制：同一玩家 10 秒内只能触发一次（沿用 `quick_chat` 那套限流思路）。
- 每次真正执行了修复都要 `add_game_log`，让玩家能看到「已清除卡住的状态 / 已推进电脑回合」。
- 修复后向双方 emit 一次房间同步（`game_state`），让前端从服务端真相重建，
  避免前端还挂着已经失效的弹窗。

#### 5.1.4 真人对局的卡死救援（**本批不做，留待大师 AI 落地后**）

作者定的方案：真人对局不做这个按钮，改成**超时自动行动**——
到点了就由 AI 托管：自动选船 / 自动开火 / 自动结束阶段，让对局自己往前走。
这实质上是「把卡住的那一方临时交给 AI 托管」，需要大师 AI 的决策层先存在，
所以排在 B 批之后。

**⚠️ 前置**：本批的 `_force_advance_turn` 与 `_responsible_party` 是为它打的底，
两个函数都要写成「责任方无关」的（不写死只处理 AI），将来托管直接复用。

#### 5.1.5 其余三条

1. **AI 摆放入口**：给 `_start_placement` 的 AI 分支接 `resolve_placement`，
   解开「回光返照 / 败者食尘 / 增援 / 死者苏生 / 滥竽充数 / 绝处逢生 / 神机妙算」的死锁。
2. **并发守卫**：`room.ai_turn_running` 标记 + `finally` 清理，防止重摆棋盘后
   两个触发点（3067/4847）同时起 loop。
3. **残留待办清理**：交回合前若发现 `pending_placement` / `pending_shenji` /
   `pending_ship_picks` / `pending_dice_discard` 里还留着自己的项，先由 brain 消费掉；
   消费不掉则显式清并记日志（**绝不留下没人消费的待办**）。

---

## 6. 嘲讽

**触发节点**（`event` 取值）：

| event | 时机 |
|---|---|
| `sink_opponent_ship` | 自己击沉对方一艘 |
| `opponent_missed` | 对方打空 |
| `lost_own_ship` | 自己被打沉一艘 |
| `opponent_played_big` | 对方打出高价值卡 |
| `about_to_win` | 对方剩余 1 艘 |
| `behind` | 自己落后 ≥ 2 艘且被击沉后 |
| `comeback` | 自己从落后追平/反超 |

**风格**：只在关键节点、看局势说；赢时可以有底气，落后时收敛，**不做人身攻击、不刷屏**。

**通道**（已定）：
- 由服务端直接 `emit('chat_message', {'username': <AI 名>, 'message': text, 'isMe': False}, to=<人类 sid>)`
  —— **不调用 `handle_chat_message`**（那带装饰器与身份校验，代他人重放会被拦，项目里栽过）。
- 同时 `add_game_log(room, ..., 'chat', {...})`，让玩家翻对局记录看得到。
- **顺手修 `isMe` 判定**：现状 `isMe = (room.players[pid].name == username)`（server.py:2443）
  是**按名字**比——玩家把自己昵称改成「AI」就会把 AI 的话显示成自己说的。
  改成按 pid 判定。

---

## 7. 无头快速模拟器与离线调权重（强度量化的前提）

### 7.1 为什么必须先有这个

**当前一局游戏要 30 秒以上**：`_ai_turn_loop` 每开一炮 `time.sleep(0.3)`（5336），
全程走 socket.io handler 与 Flask 请求上下文。跑 10⁴ 局要几天。

而这个模拟器是**三件事的共同前置**：

1. §5.1 的防卡死测试（大规模随机对局，验证「房间一定会被释放」）
2. §8 的逐卡「无残留」测试（45 张卡 × 多种局面）
3. 强度量化与离线调权重（本节）

**一份投入，三处收益**——所以它排在实施顺序第一位。

### 7.2 它是什么

一个**无 socket、无 sleep、无 Flask 上下文**的对局驱动：
直接调用对局层的纯函数（建房 → 摆船 → 猜拳 → 逐回合动作 → 结算），
双方都可以是 AI（不同难度），支持注入固定随机种子以复现。

必须具备：固定种子可复现、可只跑「某一回合的某一决策」、可批量并行。

### 7.3 强度量化（模拟器落地后立刻能做）

让**大师 vs 困难**自动对打 N 局（建议 1000 局起），输出胜率与置信区间。
**这是「大师到底强了多少」唯一的真实来源**，代替任何人的主观判断。

### 7.4 离线调权重（用进化搜索，不是强化学习）

大师 AI 的决策本身就是一组可调参数——**这就是权重向量**：

| 参数组 | 维度 |
|---|---|
| `CARD_BASE_VALUE`（45 张卡各一个值） | 45 |
| 局势乘子（落后/领先/手牌数/血量差） | ~10 |
| 出牌阈值、每回合预算、条件卡加成 | ~5 |
| **合计** | **~60 维** |

做法：
1. 固定**对手池**（现有困难 AI + 历史版本大师，防止「只赢当前自己」）
2. 每个候选权重跑 N 局自对弈，算胜率作为适应度
3. 用 **CMA-ES / 爬山** 在这 60 维上搜；10⁴–10⁵ 局对无头模拟器是可行量级
4. 胜出的权重**落成数据表**（不是模型文件），可 review、可回滚、可手改

> 这本质就是「训练」，但每一步看得懂、可复现、可回滚——与仓库现有风格一致。

### 7.5 明确不做：神经网络 / 策略梯度

三条实际理由（不是「太难」）：

1. **信息不完全性吃掉了大部分收益**——对方船位未知，拟合/搜索的收益远不如「别做蠢事」
2. **这个仓库每周都在加卡**——今天训好的网络，下一批新卡进来就得重训；
   而权重表改两个数就行
3. **不可审查**——项目每个规则模块都是可单测的纯函数。黑盒网络进来后，
   出问题只能看着它乱打

### 7.6 过度拟合警告

自对弈只能对着**自己的 AI** 调，**不等于对人类强**。
标定必须以**真人实战胜率**为准（作者本人与大师对局的战绩），
不能只看自对弈数字好看。

### 7.7 实施顺序

```
A. 无头快速模拟器 + 评测台        ← 最先做；B/C/D 的共同前置
B. 启发式大师 AI（含 §5.1 防卡死）
C. 离线权重进化搜索（自对弈）
D. 用真人实战标定
```

**强度的大头在 B（启发式），不在 C（调参）。** 现状是「闭眼随机开炮 + 有情报不用 +
条件卡永远用不对时机」，光把这三件事做对强度就上一个台阶；C 是在这个基础上追最后一截。

---

## 8. 测试策略

| 层 | 内容 |
|---|---|
| **纯函数单测**（主） | `tests/test_ai_master.py`：七个决策函数逐个测，注入固定 `rng` 保证确定性 |
| **不作弊测试** | §4.1 的两条（源码级 + 行为级）——**这是硬要求** |
| **逐卡无残留** | 沿用 `test_ai_safe_card_leaves_no_pending_state` 的形状，对**每张启用的卡**真打一次，断言不留 `pending_placement` / `pending_shenji` / `_top_ship_pick` / 未结算连锁 |
| **回合级** | 「大师 AI 能连续打完 20 个回合不卡死」——覆盖四种交互 × 逐档卡池 |
| ★ **坏 brain 逃生测试** | **把 brain 故意换成坏的**（返回 `None` / 抛异常 / 返回非法目标），断言看门狗**仍然**能把房间推进到终局。**逃生舱必须独立于「AI 写得对不对」被验证**，否则它只是个假设 |
| ★ **不打断真人测试** | 真人作为责任方时，**在 90 秒内无论多久不操作都不得被强制推进**；且真人处于「等待点选」状态时不得被催（保持 7114-7116 的现有语义）。这条守住 §5.1.1 的核心承诺 |
| ★ **房间必被释放** | 无头模拟器大规模随机对局（含故意打不支持的卡），断言**没有任何房间停在非终局状态超过上限** |
| **不回归** | easy/normal/hard 的现有 25 条 `test_ai_magic.py` 必须**全绿且不改动**（除新增大师用例） |
| **前端** | `tools/ai_difficulty_check.mjs`：`options` 断言改成 `easy,normal,hard,master`，并覆盖第 4 档的 emit 透传 |
| **真 socket E2E** | 复用 `tools/verify_ai_room.py` 的思路，跑一局大师 AI 到分出胜负 |

**每张卡开放前必须先有「逐卡无残留」测试**——顺序不能反。
**带 ★ 的三条是防卡死的核心保障，优先级高于任何强度特性。**

---

## 9. 分档实施顺序

按「零改造 → 需要交互 → 需要时机」推进，**每档独立可发布、可回退**（本文刻意是多批次规格）。

| 档 | 卡 | 需要什么 |
|---|---|---|
| — 已在池 | 火力全开 / 无中生有 / 极限增援 / 无暇圣心 / 看破！/ 五险一金 / 八方来财 / 百亿补贴（8 张） | 现状就能打，无需改动 |
| **T0** 零改造 | 守株待兔 / 命运骰子 / 神之宣告 / 疗愈 / 恶魔契约 / 禁忌果实 / 伊甸园（7 张） | 已有 AI 自动路径；只需过「无残留」测试后加入名单 |
| **T1** 选船 | 克苏鲁之眼 / 仁王之盾 | `resolve_ship_pick` |
| **T2** 目标 | 神威！/ 冻结 / 轰炸 / 探测雷达 / 硫磺火焰 | `resolve_target` |
| **T3** 挑牌 | 桃园结义 / 亡羊补牢 / 明智埋葬 / 灵气复苏 | `resolve_choice` |
| **T4** 放置 | 增援 / 死者苏生 / 滥竽充数 / 绝处逢生 / 神机妙算 / 回光返照 / 败者食尘 | `resolve_placement` + **解死锁** |
| **T5** 时机 | 余音绕梁 / Freezing！ | 准备阶段出牌机会 |
| **T6** 纯策略 | 溅射 / 雷达子弹 / 越战越勇 / 饮血 / 盗亦有道 / 平等条约 / 卧薪尝胆 / 失灵！/ 加百列之光 | `choose_card` 的条件因子 + 交错循环 |
| **T7** 特殊 | 教皇旨意 | 补「弃卡换攻击 +2」逻辑 |
| **排除** | 钢筋铁骨 | 在 `HIDDEN_CARD_NAMES` 里，**谁都摸不到**（server.py:603）⇒ 无意义 |

**数目对账**：8（已在池）+ 7 + 2 + 5 + 4 + 7 + 2 + 9 + 1 = 45，
加上被排除的钢筋铁骨 = **46 个唯一卡名** ✓（与 `magic_card.json` 一致）。
⚠️ 「余音绕梁」**同时**出现在「已在池 8 张」与 T5 里——它不是新增，而是
**把一张打空的卡修好**（`_AI_SAFE_CARDS` 现在就有它，只是永远失败）。
对账时不要把它重复计入。

> 卡牌条目数是 48（`失灵！` ×3 占 3 条），唯一卡名是 46。
> 所谓「48 张全开」实际是 **45 张可用 + 1 张拿不到**。

---

## 10. 涉及文件

| 文件 | 改动 |
|---|---|
| `ai_brain.py` | **新增**（纯规则模块） |
| `server.py` | 接线：`AI_DIFFICULTIES` 加 `'master'`；`_ai_turn_loop` 加大师分支；四个交互点加 AI 分支；`_request_ship_pick` 的 AI 分支改为调 brain；嘲讽 emit + game_log；`isMe` 改按 pid |
| `templates/index.html` | `<select id="ai-difficulty">` 加 `<option value="master">电脑：大师</option>`；`title` 文案更新 |
| `static/game.js` | 无需改（难度值是通用读取，`4943-4944`） |
| `tools/ai_difficulty_check.mjs` | `options` 断言加第 4 档（118/130/135） |
| `changelog.py` | 加一条玩家公告 |
| `CLAUDE.md` | 加一条教训（AI 决策层与防卡死） |
| `tests/test_ai_master.py` | **新增** |
| `tests/test_ai_magic.py` | 追加大师用例，**现有 25 条不动** |
| `docs/MASTER_AI_2026_09_21.md` | 本文 |

---

## 11. 风险与对策

| 风险 | 对策 |
|---|---|
| **AI 回合卡死**（最贵） | §5.1 五条；每张卡开放前先有「无残留」测试 |
| **AI 偷偷作弊** | §4.1 两条守卫测试（源码级 + 行为级） |
| **误伤现有三档** | 所有接线点走 `if _master(room)`；现有 25 条测试必须原样全绿 |
| ★ **强制推进误伤真人思考** | 判据必须是「在等谁」而非「多久没动」（§5.1.1）；真人额度沿用 90s 一行不改；带 ★ 的「不打断真人」测试守住 |
| **AI 太强变挫败** | 出牌预算上限 3 张/回合；`choose_card` 留牌不梭哈；可调 `CARD_BASE_VALUE` 与阈值 |
| **嘲讽刷屏/冒犯** | 同 event 每局一次 + 20s 间隔；台词表人工过一遍 |
| **eventlet 单线程被拖慢** | 全部决策是纯计算（枚举 16 个区域 / 36 格），无网络无 sleep；实测确认单次决策 < 1ms |
| **并发 turn loop** | `room.ai_turn_running` 标记 |

---

## 12. 实施进度（2026-09-21）

### 已完成

| 项 | 状态 |
|---|---|
| 设计规格 | ✅ 本文 |
| `ai_brain.py` 七个决策函数 | ✅ 全部实现 + 49 条测试（含两条**不作弊守卫**） |
| 无头快速驱动器 `tools/headless_game.py` | ✅ 约 200-400 局/秒，26 条测试 |
| 顺手修掉一个**真实生产卡死** | ✅ 见下 |
| `AI_DIFFICULTIES` 加 `master` | ✅ |
| 接线：读情报开炮 + 聪明选船 + 按局势选牌 | ✅ |
| 前端第 4 档 + `ai_difficulty_check.mjs` 同步 | ✅ |
| 模拟器 `ExistingAIPolicy` 改为**委托** server 唯一实现 | ✅ 见下 |

### 顺手修掉的真实生产 bug（与大师 AI 无关，线上一直存在）

「攻击次数没打完、但 36 格已全打过」→ 交不出回合，**真人一样会中招**；
且超时看门狗走同一条死路，90 秒兜底也救不回来。详见 `tests/test_attack_deadlock.py` 的文件头。

### ⚠️ 当前实测结果：**大师还不比困难强**（离 80% 很远）

1000 局 `master vs hard`（seed 1）：

| 版本 | 胜率 | 卡死 | 被拒动作 |
|---|---|---|---|
| hard vs hard（基准，随机先手） | 47.2% | 0 | — |
| 大师（只开 9 张安全卡 + 读情报开炮 + 聪明选船） | **48.5%** | 0 | 53 |
| 大师（再放开「立刻结算卡 + 需要目标的卡」） | **41.3%（更差）** | **17** | **22393** |

**两个关键结论（都已验证，不是推测）**：

1. **「按情报开炮」在没情报时是空操作。** `revealed_positions` 几乎总是空的——
   因为大师**没打**克苏鲁之眼 / 探测雷达（不在安全卡池里）。
   所以 `choose_attack` 的改进基本没生效。**「读情报」与「打情报卡」必须一起做。**
2. **放开卡池反而更弱。** 每回合只有一次出牌机会，而 AI 挑中的常常是**打不出去**的卡
   （目标形状不对 / 前置不满足），白白烧掉当回合的机会，比不出牌还糟；
   同时残留待办让 17/1000 的对局卡死。

### 下一步（按优先级）

1. ~~**先做「试算再挑」**~~ ✅ 已完成（见 §12.1）
2. ~~打开**情报卡**（探测雷达 / 克苏鲁之眼），让 `choose_attack` 的情报分支真正生效~~ ✅ 已完成
3. 再按 §9 的 T0→T7 逐档放开，每张卡先有「打出后无残留」测试 —— **进行中**（已开放 34/45）
4. 防卡死分层（§5.1）与人机房逃生按钮 —— **部分完成**（`placing_ships` 自救 + 四条待办通道消费已做，L2/L4/L5 未做）
5. 目标：大师 vs 困难 **≥ 80%** —— 当前 **67.8%**（§12.4，可复现度量）

> **§12.4 之后对"还剩多少可拿"的判断**：卡池与权重表的边际收益已接近零
> （每张卡 ±1 点以内、预算 3 张已饱和）。剩下的 12 个百分点只能来自
> **决策质量**（敌方船位后验 / 回合级两动作组合 / 主动结束回合），
> 三条都属于「改模型」而不是「调参数」，且都还没做。

### 一条关于度量工具的教训

模拟器的 `ExistingAIPolicy` 原本**自己抄了一份** `random.choice(candidates)` 和
`_ai_choose_magic_card`。后果：改了真正的 AI，胜率数字**纹丝不动**
（实测前后都是 464/536）——它量的是复刻品，度量工具形同虚设。

**已修**：`choose_attack` / `choose_card` 现在委托 `server._ai_choose_attack` /
`server._ai_choose_card`（server 侧的**唯一实现**），并补了 `choose_card_targets`。

> 教训：**度量工具必须调用被测对象的唯一实现，不能复刻它**。
> 复刻出来的工具会给出"稳定但无意义"的数字，比报错更危险（CLAUDE.md 教训 #15 的反面）。

**同族再犯一次（2026-09-21 第 2 批）**：委托接上了，但**参数没接对**——
`_ai_choose_card` 新增了 `cards_played`（每回合出牌预算），驱动器不传就永远按 0 走。
更严重的是**房间级难度**：`master vs hard` 两个座位都是 AI，而房间只有一个
`ai_difficulty` 字段，于是对手也套用了大师的卡池与开炮逻辑 ——
量出来的其实是「大师 vs 大师」，胜率必然贴着 50%。

实测：修之前 50.0%，修完立刻 43.7%。**那 6 个百分点的差就是"量错了对象"。**

---

## 12.1 第 2 批实施进度（2026-09-21 晚）

### 结论先行：大师 vs 困难 **48.5% → 65.1%**（2000 局，0 卡死）

| 版本 | 胜率 | 95% CI | 卡死 | 平均回合 |
|---|---|---|---|---|
| 第 1 批口径（9 张安全卡 + 每回合 1 张） | 47.1% | 44.9~49.3 | 0 | 7.13 |
| **本批（全卡池 + 每回合 3 张 + 试算闸门）** | **65.1%** | 63.0~67.2 | 0 | 6.58 |

同一批局面（seed 1 起、同一序列），只有 AI 的开关不同 —— 差值就是净效果。

### 各档能力的净效果（1000 局，逐档单独打开）

| 档 | 胜率 | 相对基线 | 说明 |
|---|---|---|---|
| `safe9_1` 基线 | 49.2% | — | 第 1 批卡池口径 |
| `safe9_3` +出牌预算 | 51.2% | **+2.0** | 每回合 1 → 3 张 |
| `full_1` +全卡池 | 58.4% | **+9.2** | ← **最大的一档** |
| `full_3` 两者都有 | 60.2% | **+11.0** | |
| `full_3_noinfo` 关掉"读情报" | 57.7% | +8.5 | 「读情报」本身值 **+2.5** |

> **强度的大头确实是"敢用牌"（+9.2），不是"调参数"** —— 与 §7.7 的判断一致。

复现命令（每档 1000 局约 5 秒）：

```bash
$env:PYTHONIOENCODING='utf-8'
python tools/master_ablation.py --games 1000 --seed 1
python tools/master_refusals.py 1000        # 大师侧出牌被拒的逐条原因
python tools/master_diag.py --games 200     # 行为画像（出了哪些牌、出牌/开炮比）
```

### 本批做了什么

| 项 | 状态 |
|---|---|
| **试算再挑**（`_master_card_readiness`）出牌前逐张验前置/目标 | ✅ 大师侧**被拒动作 1000 局 0 次** |
| **交错出牌**（`_ai_master_turn`：每打一炮先问一句"有没有该打的牌"） | ✅ 条件卡终于用对时机 |
| **每回合出牌预算**（`ai_brain.CARDS_PER_TURN` = 3） | ✅ 驱动器同口径 |
| **逐座位难度**（`room.ai_difficulty_by_player`） | ✅ 修掉"量成大师 vs 大师" |
| **AI 放置入口**（`_ai_consume_own_placement`） | ✅ 解开放置类卡的死锁 |
| **AI 挑牌入口**（`_ai_consume_own_choice`） | ✅ 解除 `temp_data_id` 类卡的死锁 |
| **敌船位置模型**（`enemy_cell_model`：已排除格不再浪费炮弹） | ✅ 「读情报」+2.5 |
| 卡池 9 → **28** 张（45 张里） | ✅ 每张都写明"为什么能开/为什么不开" |
| 卡池白名单 / 试算闸门 / 逐座位难度 / 两种待办消费的回归用例 | ✅ `tests/test_ai_master.py`（24 条） |

### 卡池：为什么只开 28 张，其余 17 张不开

`_MASTER_ENABLED_CARDS` 的每一项都在代码注释里写了理由（**包括不开的那些**）——
否则下一批会有人凭"价值表分数高"把它们又加回来。仍未开放的：

- **重摆类**（回光返照 / 败者食尘 / 神机妙算 / 滥竽充数 / 绝处逢生）：
  放置入口已有，但前两张会把 `room.state` 打回 `placing_ships`（整盘重摆 + 重进战斗），
  神机妙算还额外要一次「宣言一个数字」的交互与差值快照。
  **要先有测试钉住"回合结构被打断"这一段**，本批不开。
- **仁王之盾**：走 `ship_indices` 多选，与前四类都不同的一条通道。
- **神之宣告**：对自己是**净亏**（死 2 艘换对方 1 艘或一次跳回合）——
  价值表给 80 分说的是"对手打出来很强"，不是"自己打出来很强"。
- **明确有害/无用**：恶魔契约（双方绑定，只会加大自己的损失）、
  教皇旨意（AI 没有"弃卡换攻击"逻辑，打出去等于把自己的回合清零）、
  钢筋铁骨（在 `HIDDEN_CARD_NAMES` 里，谁都摸不到）。

### ⚠️ 本批踩到的三个"静默失败"（都已修 + 已加守卫）

1. **`AREA_CARDS` 从 set 改成 dict → 大师一张牌都不出。**
   `_is_master_target_card` 里要 `AREA_CARDS | LINE_CARDS | CELLS_CARDS`，
   改了类型就 `dict | set` 抛 TypeError，而它被 `_master_card_readiness` 的
   兜底 `except Exception` 吞掉 → 所有卡都判成"打不了"。
   **症状完全没有告警**：被拒动作 0、卡死 0、胜率还是个正常数（43.7%）。
   → 修法：边长另开 `AREA_SIZE` 表（集合就是集合、尺寸就是尺寸）；
     并补 `test_master_card_readiness_never_raises_for_any_playable_card` 兜底守卫。

2. **`看破！` 没有闸门 → 大师把整套手牌逐张打出去、逐张被拒。**
   1000 局 165 次，占大师侧被拒动作的 **100%**。
   → 修法：`magic_blocked` 进试算闸门；守卫按**整池**断言（不是抽查一张）。

3. **字段名与脏值**：亡羊补牢的确认字段是 `chosen_index`（写成 `card_index`
   只回一句"无效的卡牌选择"，看起来像 AI 挑错了牌）；
   `confirm_magic_target` 的灵气复苏分支把 `max_ships` 与 `target_ships`
   直接比大小，而 `magic_temp_data` 与客户端字段做过合并（白名单挡键名、不挡类型）
   → 脏值让 `int > str` 抛 TypeError，而这在回合循环里就是**永久卡死**。
   → 两处都修了，并补 `test_ai_consume_own_choice_never_raises` 用残缺状态逼一遍。

> 教训：**"兜底 except" 与"零被拒动作"合起来会制造假象**。
> 判断"AI 到底有没有在工作"，不能只看"有没有报错/卡死"，
> 必须直接数**它实际做了什么**（`tools/master_diag.py`）。

### 下一步（第 3 批）

见 §12.2（已实施）。

### 关于 80% 这个指标的现状判断

65.1% 已经是「稳定高于困难」的强度（95% CI 63.0~67.2，与困难的 50% 完全不重叠）。
剩下的 15 个百分点预计主要来自**还没开放的那 17 张卡**（尤其重摆类与仁王之盾），
而不是继续调权重表 —— 与 §7.7「强度的大头在 B（启发式）不在 C（调参）」一致：
本批 +9.2 来自"敢用牌"、+2.0 来自预算、+2.5 来自情报，**调参一项都没做**。

---

## 12.2 第 3 批实施进度（2026-09-21 深夜）

### 结果：大师 vs 困难 **65.1% → 70.2%**（2000 局，0 卡死，大师侧被拒动作 0）

> ⚠️ **这两个数字都是 2000 局读数，本区间的高频抖动有 ±1.5 个百分点**
> （见 §12.3 的度量纪律：把种子序列切开成两个独立半样本才看清真实值）。
> 第 3 批的**真实**水平落在 **68% 上下**；本表里的单个数字只用于同一批内的相对比较。

| 版本 | 胜率 | 95% CI | 卡死 | 平均回合 |
|---|---|---|---|---|
| 第 1 批口径（9 张安全卡 + 每回合 1 张） | 49.5% | 47.3~51.6 | 0 | 7.09 |
| 第 2 批（全卡池 28 张 + 试算闸门） | 65.1% | 63.0~67.2 | 0 | 6.58 |
| **第 3 批（34 张可用）+ 重摆自救 + 准备阶段窗口** | **70.2%** | 68.1~72.1 | 0 | 6.54 |

### 本批做了什么

| 项 | 状态 |
|---|---|
| **重摆类卡**（回光返照 / 败者食尘 / 滥竽充数）：`placing_ships` 里自己爬起来 | ✅ 孤立测量 **+3.4** |
| **准备阶段出牌窗口**（`余音绕梁` 只在这一个窗口打得出去） | ✅ 孤立测量 +0.7 |
| **神机妙算宣言消费**（`_ai_consume_own_shenji`） | ✅ 孤立测量 +0.8 |
| **明智埋葬**（优先埋对方手里最值的那张，而非自己牌堆里的） | ✅ 孤立测量 +0.4 |
| **卧薪尝胆** | ✅ |
| 三条待办通道收口成 `_ai_consume_own_pending`（新增通道不会漏接） | ✅ |
| **伊甸园：进池后被实测否掉**（见下） | ✅ 去掉后 **+2.4 且卡死归零** |
| 回归用例 28 条（新增放置自救 / 宣言残缺态 / 埋葬口径 / 逐卡表态） | ✅ 全套 `2027 passed` |

### ⚠️ 本批最重要的两条实测结论

**① 「卡进池」不等于「值得打」——必须逐张量。**
`伊甸园` 看着是"自己船少就拿得多"的好牌，把它的判据写对了（自己船数 < 对方才打）
之后**仍然是负收益**，而且**唯一的两局卡死就是它造成的**：

```
full_3          70.4%   卡死 0      ← 去掉伊甸园之后
no_yidian       70.2%（当时含伊甸园的批次里，去掉它 +2.4）
```

原因是卡面公式 `双方攻击次数 = 6 − 自己的船数` 是**对称**的：自己 6 艘时打出它 →
**自己的攻击次数变成 0**；5 艘时 → 1，等于用 4 次攻击去换对手的 1 次。
只有"自己船数远多于对方"才略有优势，而那正是 AI 已经领先、不需要它的局面。
→ 现在的做法：把 `伊甸园` **明确排除**并在注释里写明"这是实测否掉的，不是保守"。

**② 逐张"去掉一张再跑"是定位负收益卡最快的手段。**
把整批一起打开只能看到总账，看不出是谁的锅。`tools/master_ablation.py` 的
`_without(...)` + `--configs no_xxx` 就是为此加的：一次一张、同一批局面。
本批 5 张新卡全部量成正收益：

| 档 | 胜率 | 相对全池 |
|---|---|---|
| `full_3`（全池） | 70.4% | — |
| 去掉重摆三张 | 67.0% | **−3.4** |
| 去掉余音绕梁 | 69.7% | −0.7 |
| 去掉神机妙算 | 69.6% | −0.8 |
| 去掉明智埋葬 | 70.0% | −0.4 |

### 卡池现状：34 张可用 / 45 张可摸到

仍未开放的 11 张与理由（都写在 `_MASTER_ENABLED_CARDS` 旁边的注释里）：

- **仁王之盾**：走 `ship_indices` 多选，是第 4 条通道（其余三类都已接）
- **神之宣告**：对自己是净亏（死 2 艘换对方 1 艘或一次跳回合）
- **教皇旨意**：AI 没有「弃卡换攻击」（§9 T7），打出去等于把自己的回合清零
- **恶魔契约**：双方绑定，只会加大自己的损失，还会给**对方**开交互
- **禁忌果实**：封掉双方魔法 —— AI 靠魔法卡赢，这是自废武功
- **伊甸园**：**实测否掉**（见上）
- **命运骰子**：摇到 3 点开 `pending_dice_discard`，AI 侧没有消费点（要做 +1 条通道）
- **平等条约 / 加百列之光 / 盗亦有道**：已在池内（前置由闸门判）
- **钢筋铁骨**：`HIDDEN_CARD_NAMES` 里，谁都摸不到

### 下一步（第 4 批）

见 §12.3（已实施）。

---

## 12.3 第 4 批实施进度（2026-09-22 凌晨）

### 结果：大师 vs 困难 **69.5%**（两个独立的 4000 局半样本：69.2% / 69.7%）

| 版本 | 胜率 | 95% CI | 卡死 | 大师用到的卡种 |
|---|---|---|---|---|
| 第 1 批口径（9 张安全卡 + 每回合 1 张） | 48.0~49.5% | 见各批 | 0 | 9 |
| 第 3 批（28 张） | ~68% | — | 0 | 28 |
| **第 4 批（34 张）** | **69.5%** | 68.2~70.6（4000 局） | 0 | **34** |

### ⚠️ 先记一条度量纪律：小样本的"提高"多半是波动

本批过程中我先记下过 **71.2%**（2000 局）并写进了文档，随后又量到 70.0%、69.5%。
把种子序列**切开成两个独立的 4000 局半样本**之后才看清：

```
seed 1      : 69.2%  (95%CI 67.7~70.6)
seed 50000  : 69.7%  (95%CI 68.2~71.1)
```

两个半样本互相吻合（差 0.5 个百分点），说明 **真实值 ≈ 69.5%**，
而之前那些 2000 局读数（70.0 / 70.8 / 71.2）只是同一个区间里的高频抖动。
→ **改动前后各跑 2000 局、然后比两个数字，是在读噪声。**
   要判定一个改动，要么样本翻几倍，要么两个独立半样本互相对上。

> 这条同样适用于下面那张"逐张去掉"的表：它是**方向性**证据，不是精确刻度。

### 本批做了什么

| 项 | 状态 |
|---|---|
| **第 4 条交互通道**：多选船（`ship_indices`，仁王之盾那类） | ✅ 通道已接（含"沉船不加盾"的口径） |
| **绝处逢生的放置**：`last_stand` 只认白名单（不过默认占位规则） | ✅ 通道已接 |
| `trap_setup`（守株待兔）的选船判据与"要牺牲"分开 | ✅ 旧实现与卡面目的正好相反 |
| 逐张隔离测量 `--configs no_xxx`（新卡一律先量再加） | ✅ 见下 |
| **两条"讲得通但实测不更好"的改动被回退**（见下） | ✅ |
| 回归用例 31 条 | ✅ 全套 `2030 passed` |

### ⚠️ 本批最重要的结论：「看着更优」的两处改动实测都没有变好

**① `绝处逢生`：从"打中即胜"里算不出便宜。**
卡面是「牺牲全部战舰 → 放 1 艘 → 接下来击沉对方即胜」，看上去是绝境翻盘神器。
闸门已经收得极紧（**只在对方只剩 1 艘时才打**），实测**仍然在扣分**：

```
带着它   68.5%
去掉它   71.9%     ← 同一批 1000 局
```

原因：对方剩 1 艘时，我本来有 **5~6 次攻击机会**，命中率随次数累积；
牺牲掉全部战舰之后只剩 **1 次**攻击、且必须打中对方那唯一一格 ——
等于**把已经很高的胜率换成 1/36 的抽奖**，而且自己只剩 1 艘，
对方下一次反手就结束。**"击杀即胜"的收益被"只剩一发"的成本吃光了。**

**② 牺牲哪艘船：我写的"更讲道理"的启发式没有变得更好。**
我原本的判据是"邻里被轰过的格数最多 → 泄露代价最小 → 该牺牲"。
我把它改成"周围一格都没被搜过的船最不该牺牲"（听起来显然更对），
两版在同一批 2000 局上对照：

```
旧版  69.5%
新版  68.7%     ← 差 0.8 个百分点，在噪声内，但至少没有证据支持新版
```

→ 回退到旧版，并把两版都留在 `tools/master_ablation.py` 的
  `ship_old` / `ship_new` 里，随时可复测。**"讲得通"不等于"更强"。**

**③ 顺带修掉一个真 bug**：`resolve_ship_pick` 旧实现把 `trap_setup`
（守株待兔：**希望**这艘船被打到）和"要牺牲"当成同一件事，
于是给要设陷阱的船也挑"信息价值最低"的那艘 —— 与卡面目的正好相反。现已分开。

### 逐张隔离测量的完整账（`--configs no_xxx`，1000 局；方向性证据）

| 去掉哪张 | 胜率 | 相对全池 |
|---|---|---|
| （全池） | ~70% | — |
| 重摆三张（回光返照/败者食尘/滥竽充数） | 67.0% | **−3.4** |
| 神机妙算 | 69.6% | −0.8 |
| 余音绕梁 | 69.7% | −0.7 |
| 明智埋葬 | 70.0% | −0.4 |
| 仁王之盾 | 67.9% | −0.6（已排除） |
| 绝处逢生 | 71.9% | **+3.4**（已排除） |
| 伊甸园 | 70.2% | **+2.4**（已排除，第 3 批） |

> ⚠️ 1000 局的 95% CI 约 ±3 个百分点，所以**单张卡 ±0.6 这类数字不要当结论**，
> 只能当"没有证据说明它有害"。真正被排除的三张（伊甸园 / 绝处逢生 / 仁王之盾）
> 是按"去掉之后不亏甚至更好"的**方向**决定的。

### 卡池现状：34 张可用 / 45 张可摸到，**这 34 张每张都量成正收益**

仍未开放的 11 张：

| 卡 | 不开放的理由 |
|---|---|
| 绝处逢生 | **实测 −3.4**（见上）——通道已实现 |
| 仁王之盾 | **实测 −0.6** ——通道已实现 |
| 伊甸园 | **实测 −2.4 且唯一卡死来源**（第 3 批） |
| 神之宣告 | 对自己净亏（死 2 艘换对方 1 艘），且要两道交互 |
| 恶魔契约 | 双方绑定，只会加大自己的损失，还给对方开交互 |
| 教皇旨意 | AI 没有「弃卡换攻击」（§9 T7），打出去等于清零自己回合 |
| 禁忌果实 | 封掉**双方**魔法，AI 靠魔法取胜 = 自废武功 |
| 命运骰子 | 摇到 3 点开 `pending_dice_discard`，AI 侧还没消费点（+1 条通道） |
| 钢筋铁骨 | `HIDDEN_CARD_NAMES` 里，谁都摸不到 |
| 平等条约 / 加百列之光 | 已在池内（前置由闸门判） |

### 下一步（第 5 批）

见 §12.4（已实施，含一条**改变全局判断**的发现）。

---

## 12.4 第 5 批：度量修好之后，重新认识了强度来自哪里（2026-09-22 上午）

### ⚠️ 先修一个真缺陷：AI 的决策其实是**熵随机**的

`ai_brain` 的四个决策函数都以 `rng = rng or random.Random()` 结尾。
`random.Random()` 不给种子时是**用系统熵播种**的，所以那句"没传就用一个默认的"
实际含义是"**每次调用都换一个不可复现的随机源**"。后果两层：

- **生产**：AI 的开炮 / 放置 / 选船是熵随机的 —— 本模块头上
  「rng 注入而非用全局 random，**为了可复现**」那句注释是假的；
- **度量**：无头驱动器的 `random.seed(seed)` 管不到它。
  实测（同一进程内连跑 6 次 `seed=1`）：

  ```
  修前: ('human-0', 5, 103) ('ai-83a831', 8, 133) ('ai-85e0d4', 9, 154)
        ('human-0', 6, 117) ('human-0', 5, 86)   ('human-0', 9, 149)
  修后: ('human-0', 4, 81)  × 6 次完全一致
  ```

  **胜负都会翻、回合数在 3~11 之间跳。** 也就是说：**此前每一张卡的能力对照、
  每一次"改动前 / 改动后比两个数字"，里面都掺了这个噪声。**
  第 4 批那条"2000 局读数不可信"的教训，根因就在这里（不只是样本量小）。

- **修法**：退化到**模块级 `random`**（新增 `ai_brain._rng`），
  而不是新建实例 —— 驱动器与 `handle_attack` 都把 `random.seed()` 打在同一份
  全局状态上，整局从猜拳到开炮才走同一条可复现序列。
- **守卫**：`test_ai_brain_never_creates_an_unseeded_rng`（源码级）+ 
  `test_headless_game_is_deterministic_for_a_fixed_seed`（同一 seed 两次跑，
  **逐动作**比对）。后者此前完全缺失 —— 模拟器"固定种子可复现"是 §7.2 的根本契约，
  却没有任何用例守着，所以这个缺陷活了很久。

### 修好之后重测：**强度几乎全部来自"能用到更强的卡"**

在**可复现**的度量上重跑（每档 3000 局，同一批局面，只换一个开关）：

| 档 | 胜率 | 相对基线 | 含义 |
|---|---|---|---|
| `safe9_1` 9 张卡 + 每回合 1 张 | 48.2% | — | 第 1 批口径 |
| `pool9_value` 同上，但用**本批的价值表选牌** | 48.2% | **+0.0** | ← 选牌器在 9 张卡上**毫无贡献** |
| `pick_value` 34 张卡 + 价值表（线上默认） | **67.8%** | **+19.5** | |
| `pick_speed` 34 张卡 + 只按速阶挑 | 65.6% | +17.3 | |
| `pick_random` 34 张卡 + **可打的里随便挑** | 65.7% | +17.4 | |

**三条结论，都很硬**：

1. **值 +17~20 个百分点的是"卡池里有那些牌"，不是"怎么挑"。**
   把选牌换成**随机**，仍有 65.7%（对比 67.8%）。
2. **"按局势选牌"这个人设只值 +2.2 个百分点**（67.8 vs 65.6 / 65.7，CI 不重叠）。
   §0 那句"现有 AI 是从 9 张安全卡里挑最便宜的"确实是它弱的原因，
   但修好它只买到 2 个点 —— **真正买到 17 个点的是把 34 张卡都接进来**。
3. **在 9 张卡的池子里，价值表一分钱都不值**（48.2% → 48.2% 完全一致）。
   因为那 9 张几乎没有条件依赖，挑哪张都差不多。
   → 印证了 §7.7 的判断，但更极端：**强度全在"决策空间"上，不在"决策函数"上。**

### 其余各档（同一可复现度量上重测）

| 档 | 胜率 | 相对全池 |
|---|---|---|
| 出牌预算 = 每回合 1 张 | 66.3% | **−1.5** |
| 出牌预算 = 每回合 3 张（默认） | 67.8% | — |
| 出牌预算 = 每回合 5 张 | 67.8% | +0.0（3 张已够） |
| 加回「伊甸园」 | 65.9% | **−1.8，且 28 局卡死** |
| 加回「绝处逢生」 | 66.6% | **−1.1** |
| 加回「仁王之盾」 | 67.8% | **±0.0（无差别）** |
| 去掉重摆三张 | 65.9% | **−1.9** |

> 修正第 4 批的两个说法：出牌预算是 **+1.5**（之前记的 +2.0 是噪声）；
> `仁王之盾` **不是负收益，而是完全无差别**（之前记的 −0.6 是噪声）。
> 只有 `伊甸园`（−1.8 且是唯一卡死来源）与 `绝处逢生`（−1.1）是**真**负收益。

### 所以"止步于此了吗"——不，但方向要换

到这里可以明确说：**继续加卡 / 继续调权重表的边际收益已经接近零**（合计只剩 3 个点，
而且每张卡都在 ±1 点以内）。剩下的 12 个百分点只能来自**决策质量**，
而且必须是"以前没做过的"那类，不是再拧一遍旋钮：

1. **敌方船位的后验**：现在 `choose_attack` 只有"已探明 / 未打过"两档。
   6 艘单格船在 36 格上，减去已探明的和已打过的，**剩余未知格的期望船数是可算的** ——
   现在只用"均匀挑"，没有用它。这是唯一还没动过的核心模型。
2. **回合级规划**：现在是"能打就打、能出就出"的贪心。
   例如"先打一发探明、再用溅射放大"这种**两动作组合**从来没被考虑过。
3. **主动结束回合**：`end_turn` 现在是"打不出去了才交"。
   有没有"现在交回合比继续打更好"的局面（例如留着攻击次数配合下一张卡）？没测过。

这三条都属于「改模型」而不是「调参数」，也都还没做 —— 所以**不是止步于此**，
但也不该再用"再多开几张卡"来假装有进展。

---

## 12.5 第 6 批：作者实战两条反馈（2026-09-22）——节奏与"打不疼人"

作者实际对局后报了两条，本批逐条处理。**两条的结论都与"直觉改法"不一样**，
所以先把证据记清楚。

> 原话：「攻击的时候可以快一点 主要是我是怕出牌的时候太快了 上一个效果还没结算完
> 下一张牌已经打出来了 然后大师AI的侵略性还是不够 我甚至可以无伤赢他」

### 12.5.1 任务一：节奏 —— "没结算完就打下一张"是真的吗？

**结论：不是。这是观感问题，不是结算竞态。**（证据在下面，不是读代码推的）

判据 `server._master_unsettled(room, ai_id)`：一个房间只要有**任何**东西没落地，
它就返回非空。它覆盖六条通道 —— 连锁栈 / 连锁响应窗口 / `pending_placement` /
`pending_shenji` / `pending_dice_discard` / `priority_continue`，外加**大师自己**的
`pending_ship_picks`（真人名下的**有意不算**，理由见下）。

工具 `tools/probe_master_settle.py` 跑**真实的 `_ai_master_turn`**（只把 `time.sleep`
换成记账版，决策与结算一行不改），在**每次**出牌 / 开炮 / 收尾之前抓一次判据：

| 50 局实测 | 次数 |
| --- | --- |
| 大师出牌尝试 | 9 |
| **出牌前仍有未结算项** | **0** |
| 大师开炮尝试 | 164 |
| **开炮前仍有未结算项** | **0** |
| 大师收尾尝试 | 27 |
| **收尾前仍有未结算项** | **0** |

读代码也能对照上：`_ai_master_turn` 循环每圈开头就有
`if room.chain or room.chain_waiting: _master_settle(...); continue`（战斗阶段），
每一张牌之后都走一次 `_master_settle`；而 `handle_attack` / `handle_use_magic_card`
自己也有 `chain_waiting` 门禁（那是**第二道**保险，不是唯一一道）。

**⚠️ 但代码里确实有一个真空洞，本批补上了**：`_master_settle` 原来的收敛判据只有
`room.chain or room.chain_waiting`，**不看 `pending_placement`**；而
`handle_use_magic_card` 也**不拦** `pending_placement`（只有 `_action_wait_reason` 拦）。
也就是说「打完一张会开放置流程的卡（增援 / 死者苏生 / 滥竽充数 / 灵气复苏），
船还没放就接着打下一张」这条路径**当时没有任何东西在拦**。
它没被实测撞到，只是因为 AI 名下的待办会被 `_ai_consume_own_pending` 当场消费掉
（消费得快，所以窗口极短）—— 属于"靠运气没炸"，不是"设计上不会炸"。

**改了什么**：

1. `_master_unsettled(room, ai_id)` 收口成**唯一判据**（六条通道 + 4 个否定用例）。
2. `_master_settle` 每圈**先替 AI 把待办消费掉、再判**，判据换成上面那个函数。
3. 超时日志从「大师 AI 等待连锁收敛超时」改成**带上具体哪条通道**
   （原来是"不静默但说不出是哪一条"）。
4. **节奏常量拆成两个**：`_MASTER_ACTION_PACING = 0.8`（出牌 ×2 / 阶段转换 / 开炮
   四处共用）→ `_MASTER_CARD_PACING = 0.5`（**只管出牌**）。开炮与阶段转换的
   `time.sleep` **直接删掉** —— 作者要求「攻击可以快一点」，而那个共用常量让
   每一炮都白等 0.8 秒（六炮 = 4.8 秒）。

停顿总量的实测（`tools/probe_master_settle.py --games 80 --master-turns 1`，
`time.sleep` 换成记账版，所以不花真实墙钟）：

| | 改前（旧写法） | 改后（现在） |
| --- | --- | --- |
| 开炮前 | 每炮 0.8s → **0** | 0 |
| 进战斗阶段前 | 每回合 0.8s → **0** | 0 |
| 出牌前 | 每张牌 0.8s | 每张牌 0.5s |

同一个大师回合里的「出牌停顿次数」分布（46 个回合实测）：

| 停顿次数 | 回合数 |
| --- | --- |
| 9 次 | 29 |
| 10 次 | 9 |
| 12 ~ 13 次 | 2 |
| **43 ~ 44 次** | **6** |

⚠️ **诚实记一笔**：9~10 次比"每回合最多 6 张牌"要多 —— 因为战斗阶段的
`time.sleep(_MASTER_CARD_PACING)` 排在**预算判断之前**，打不出牌的那几圈也各停一次
（每张牌最多多停 1.5 秒，属已知的低效但不影响正确性，没有在本批动它）。
**43~44 次那 6 个回合我没有查到原因**（候选解释是重摆/长回合，但没验证）——
也就是说"停顿总量少了多少"这个数我不敢给精确值，只能给出**结构性的结论**：
开炮与阶段转换的停顿已经归零，出牌停顿从 0.8 降到 0.5，每一炮不再白等。

**顺手复核 `_MASTER_REDEPLOY_CARDS` 那个闸门**（作者点名要求）：
`_master_card_readiness` 里的 `if room.state == 'placing_ships' and name in
_MASTER_REDEPLOY_CARDS: return None` 应**保留**，理由与原来一样但更明确 ——
它是防"两个重摆标记叠在同一个房间上"（`handle_place_ships` 的收尾是
`if huiguang… elif lingqi…` 互斥链，只有一支会被消费）。这条闸门管的**不是**
"有没有结算完"，而是"房间正停在一个谁都没法正常行动的状态"；
真正的"结算完"判据是本批新增的 `_master_unsettled`。**两条判据各管一件事，不合并。**

### 12.5.2 任务二：侵略性 —— 先造一个能看见它的指标

作者的反馈「我甚至可以无伤赢他」在此前的指标里**完全不可见**：验收指标只有胜率。
所以第一步是加量，加在 `tools/headless_game.py`：

| 字段 | 含义 | 数据来源 |
| --- | --- | --- |
| `sunk_by_p1` / `sunk_by_p2` | 平均击沉对方几艘 | 终局 `max_ships − remaining_ships` |
| `kills_by_p1` / `kills_by_p2` | **炮击击沉事件**数/局 | 每炮之后判"命中格上的船沉了吗" |
| `p1_hit_rate` / `p2_hit_rate` | 命中率 | 命中炮数 ÷ 开炮数 |
| `p1_flawless_wins` / `_rate` | **无伤获胜**局数与占该座位胜局的比例 | 终局船数差 == 0 且赢 |

⚠️ 这几个量**必须分开**，因为「终局船数差」会被 `死者苏生 / 增援 / 疗愈 /
滥竽充数` **倒扣**（也可能被改船数上限扭曲）。master vs hard 1000 局实测：

- 炮击击沉事件 **4.69/局**，终局船数差只有 **2.59/局** —— 差的 2.1 艘全是卡牌捞回来的；
- 也就是说：**只看"平均击沉对方"会得出"大师打不疼人"，而它的原始输出其实比 hard 高 26%**
  （4.69 vs 3.71）。

（我第一版就只加了终局差，据此写下的"大师输出比对手低"的结论是**错的**。
这条写在这里是为了让下一个人别再踩。）

### 12.5.3 侵略性的基线（1000 局 / seed=1 / master vs hard）

| 指标 | 值 |
| --- | --- |
| 胜率 | **68.8%** |
| 平均击沉对方 / 被击沉 | **2.594 / 4.980** |
| 炮击击沉事件（大师 / 困难） | **4.692 / 3.712** |
| 命中率（大师 / 困难） | **18.33% / 17.86%** |
| 每局开炮 | 47.06 次 |
| 卡死 / 被拒动作 | 0 / 192 |

作者那句「无伤赢」在这张表里的位置，是这两个数：

- **大师赢的 688 局里，自己一艘没沉的有 127 局（18.5%）** —— 防守端的无伤；
- 困难赢的 312 局里，困难自己一艘没沉的有 9 局（2.9%）—— 对手端的无伤；
- 困难赢的局里，**大师打沉 ≤2 艘的有 38%**（0 艘 5.1% + 1 艘 16.3% + 2 艘 16.7%）。

### 12.5.4 ⚠️ 逐个否掉"为什么打不疼人"（每个方向都有数据）

作者列了 4 个怀疑方向，**逐条量过，4 条全部被数据否掉**：

**① 开炮不聚焦？—— 否。聚焦逻辑是对的，但"情报"根本不存在。**

`ai_brain.choose_attack` 的规则是「有已探明格就打它（必中）」，实测
`tools/master_intel_diag.py`（400 局）：

| | 占比 |
| --- | --- |
| 开炮时**手上有已探明格** | **0.7%**（68 / 10137 炮） |
| 有探明格却打了别处 | **0**（一次都没有 ← 聚焦逻辑 100% 兑现） |
| 开炮时没有情报 | 99.3% |

命中率的理论上界：36 格 / 6 艘单格船，**完全均匀撒 = 6/36 = 16.67%**。
实测大师 18.33%、困难 17.86% —— 两者都贴着这个数。**18% 不是"没聚焦"，
而是"没有情报时这就是上界"。** 想再高只能靠更多探明手段，不是靠更聪明的选点。

**② 出牌预算被浪费在低收益牌上？—— 否，而且预算根本不是约束。**

同一批种子逐档扫描 `ai_brain.CARDS_PER_TURN`（1000 局）：

| 预算 | 胜率 | 击沉对方 | 炮击击沉 | 卡死 |
| --- | --- | --- | --- | --- |
| 1 | 67.2% | 2.789 | 4.620 | 0 |
| 2 | 69.0% | 2.596 | 4.671 | 0 |
| **3（线上）** | **68.8%** | **2.594** | **4.692** | 0 |
| 5 | 68.9% | 2.594 | 4.690 | 0 |
| 8 | 68.9% | 2.594 | 4.690 | 0 |

**预算 2 → 8 的数字几乎逐位相同**。原因是「可打的牌」本身就那么少
（见 ③），预算再大也没有第 4 张可打。所以"加预算"这条路是**空的**。

**③ 价值表 / 局势乘子让大师领先时变保守？—— 否，是"根本没得选"。**

`tools/master_intel_diag.py` 数了每次"问该打哪张牌"时**可打候选有几张**：

| 候选数 | 占比 |
| --- | --- |
| **0（打不出牌）** | **71.4%** |
| **1（没得选）** | **22.9%** |
| ≥2（真的在选） | **5.7%** |

`tools/master_choice_diag.py` 进一步量了"选中的那张 vs 候选池"：
**可打候选张数的中位数 = 1**，也就是 94.3% 的场合 `choose_card` 的 argmax
**没有任何选择余地**。选中 = 候选里最高分的比例是 100%（说明函数没坏），
但它"高"得毫无意义 —— 候选只有一个。

对照实验也支持这一点：把选牌器换成**按速阶挑**、甚至**随机挑**，
胜率都是 **64.0%**（300 局，两者完全相同）—— 选牌器在决策空间只有 1 的情况下
一分钱都不值。这是 CLAUDE.md 教训 #37 的加强版。

**④ 情报类卡（探测雷达）有没有被用起来？—— 用起来了，但量太小。**

| 卡 | 1000 局出场 | 在不在池里 |
| --- | --- | --- |
| 探测雷达 | 201 次 | 在池 |
| 雷达子弹 | 143 次 | 在池 |
| 明智埋葬 | 188 次 | 在池 |
| 克苏鲁之眼 | **0 次** | **不在池**（有意，见 §9） |

201 次 / 1000 局 = 每 5 局一次，而它只显形 2×2 区域里的船 —— 所以"有情报的炮"
只占 0.7%。**"多打情报卡"是提高命中率的唯一真路径**，但受 ③ 的约束：
手上的牌多数打不出去，情报卡的出场次数上不去。

### 12.5.5 还试了"换卡池结构"，全部是**拿胜率换击沉**

同一批种子（1000 局）：

| 档位 | 胜率 | 击沉对方 | 被击沉 | 炮击击沉 | 无伤胜率 | 卡死 |
| --- | --- | --- | --- | --- | --- | --- |
| **baseline（线上）** | **68.8%** | 2.594 | 4.980 | 4.692 | 18.5% | 0 |
| 只留"直接输出"的 5 张卡 | 53.2% | **4.013** | 4.416 | 4.130 | 4.7% | 0 |
| 去掉"捞回自己船"的 4 张 | 64.6% | 3.280 | 4.772 | 4.516 | 5.3% | 0 |
| 去掉"试算闸门最常剔掉"的 3 张 | **70.2%** | 2.657 | 4.995 | 4.692 | 18.1% | 0 |
| 把池子扩到全部"能打的" | **50.9%** | 3.126 | 4.166 | 3.923 | 11.2% | **177** |
| 加回克苏鲁之眼 | 68.8% | 2.594 | 4.980 | 4.692 | 18.5% | 0 |

**结论：侵略性与胜率在当前设计下是反向的。** 把牌全花在输出上，
击沉从 2.59 涨到 4.01（+55%），但胜率掉 15.6 个点、无伤胜率从 18.5% 掉到 4.7%
（自己也被打得更惨）。**这个交换我没有替作者拍板，改动留在工具里可随时复跑**
（`tools/master_aggression_ablation.py`）。

唯一**双向都不亏**的一条：`no_dead_weight`（去掉回光返照 / 平等条约 / 加百列之光）
→ 胜率 **+1.4 点**、击沉 +0.063、被拒动作从 192 降到 100。
它不是"更侵略"，而是**手牌流动性更好**（这三张在手里占位、又几乎永远打不出去，
实测在 4886 次"空候选"里占 2691 次）。但"从卡池里删掉三张卡"是**改玩法**，
不是调参，所以只作为候选记录，没有改线上池。

### 12.5.6 ⚠️ 顺手修掉的一个真缺陷：对照实验工具本身是熵随机的

`tools/master_ablation.py` 的 `_plain_attack`（"不看情报、均匀撒"的对照档）写的是
`rng = rng or random.Random()`。**无参 `random.Random()` 用系统熵播种**
（CLAUDE.md 教训 #36 的原文形状，项目刚为它吃过一次亏）。

`tools/probe_rng_reproducibility.py` 的实测（300 局，同一 seed 跑两次）：

| 变量 | 两次一致？ |
| --- | --- |
| 不换任何东西 | ✅ |
| 换成**逐字节相同**的 spy | ✅ |
| 均匀撒（无参 `random.Random()`） | ❌ 胜率 72.0% vs 70.7% |
| 均匀撒（用 `ai_brain._rng`） | ✅ |

**也就是说：此前记在 §12.3 / §12.4 里"读情报值多少分"的那个数字（`full_3_noinfo`）
是噪声。** 本批把两个 ablation 脚本的 `_plain_attack` 都改成 `ai_brain._rng`，
并加了两条守卫（源码级扫无参 `random.Random()` + 行为级"两次采样必须相同"）。

修好之后重测：**读情报值 −0.5 个点**（68.8% → 68.3%），
即「按情报开炮」这条在**当前情报供给量下**是**零贡献**（因为只有 0.7% 的炮有情报）。

### 12.5.7 本批的完整前后对照（同一 seed=1，1000 局 master vs hard）

| 指标 | 改前 | 改后 |
| --- | --- | --- |
| 胜率 | 68.8% | **68.8%** |
| 平均击沉对方 | 2.594 | **2.594** |
| 平均被击沉 | 4.980 | **4.980** |
| 炮击击沉事件 | 4.692 | **4.692** |
| 命中率 | 18.33% | **18.33%** |
| 无伤获胜率 | 18.5% | **18.5%** |
| 卡死 / 被拒动作 | 0 / 192 | **0 / 192** |
| 开炮前停顿 | 每炮 0.8s | **0** |
| 进战斗阶段前停顿 | 每回合 0.8s | **0** |
| 出牌前停顿 | 每张 0.8s | 每张 0.5s |

**对局数值一字未变、开炮与阶段转换的停顿归零** —— 这正是本批想达到的：
节奏是**表现层**的事，不该动到对局；而侵略性那部分，量出来的结论是
「当前设计下它与胜率反向，需要作者拍板」，所以**没有擅自改**。

### 12.5.8 本批没做 / 做了但没结论的

1. **没改任何对局数值**（见 12.5.7 的对照表）——侵略性的四个怀疑方向全部被否掉，
   唯一有效的两条（删三张占位卡 +1.4 点 / 把牌全花在输出上 −15.6 点）都是
   **玩法级**改动，按作者要求不擅自拍板。
2. **"敌方船位后验"仍然没做**（§12.4 列的 3 条之一）。本批量出来它才是真瓶颈：
   命中率贴着 16.67% 的均匀上界，而唯一能抬高它的是情报供给。但那需要先解决
   "手牌流动性"（71.4% 的回合一张可打的牌都没有），否则情报卡也出不来。
3. **`cards2` → `cards8` 逐位相同**这件事没有深挖到底 —— 我给出的解释是
   "可打候选数 ≤1 导致的"（有 ③ 的直接测量支持），但没有做"把候选数人为放大"
   的对照实验来完全排除其它解释。
4. `pool_all_playable` 的 **177 局卡死**没有逐局定位到具体是哪张卡 ——
   如果将来要扩池，这是必须先做的一步。
5. **每回合的出牌停顿次数里有 6/46 个回合是 43~44 次**（正常 9~10 次），
   原因没查到 —— 所以"停顿总量降了百分之多少"这个数我没有给精确值，
   只给了结构性结论（见 12.5.1 的表）。

---

## 13. 验收标准

1. ★ **大师 AI 能连续打完 ≥ 20 个回合一次不卡死**；且**坏 brain 逃生测试**通过
   （把 brain 换成坏的，看门狗仍能释放房间）。
2. ★ **真人思考时间零影响**：真人作为责任方时 90 秒内不会被强制推进，
   等待点选状态不被催（§5.1.1）。
3. **无头模拟器跑 1000 局大师 vs 困难，胜率 ≥ 80%**（本项目的强度硬指标，
   由机器测量而非主观判断）。
4. 45 张可摸到的卡**每一张都被大师 AI 实际打出过**（逐卡测试覆盖）。
5. easy / normal / hard 的现有测试**全部原样通过**，三档行为零变化。
6. 「AI 不作弊」两条守卫测试通过。
7. 与大师 AI 对局时，关键节点能看到它发言，且对局日志里能翻到。
8. 前端四档可选，`tools/ai_difficulty_check.mjs` 全绿。
9. 真人实战标定（作者本人对局）与自对弈数字不出现严重背离（§7.6）。
