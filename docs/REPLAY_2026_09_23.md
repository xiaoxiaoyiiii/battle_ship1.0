# 对局回放（2026-09-23）

> 需求：在自己/别人的**战绩**里点开某一局 → 「对局回放」按钮 → **全透视**回放
> （双方船位 + 双方手牌都可见）、**以每次行动为一步**、可暂停/上一步/下一步/倍速、
> 进度条标出关键节点（使用某卡、击沉战舰）、鼠标移到节点能看那一步发生了什么。
> **没有思考时间**，固定速度播放。

---

## 0. 一句话设计

**回放 = 现有 `game_logs` 的步骤序列 + 两条稀疏时间线（船位 / 手牌）+ 棋盘重置标记。**
不存每步全量快照（那会让体积涨 5~10 倍），棋盘标记按时序从 attack 步反推（与观战同一套朝向逻辑），
再叠上重置事件。落库走**独立表 + 独立按需接口**，**绝不塞进 `match_logs.logs`**（那一列会随
`show_history` 公开给所有人，而且战绩接口每次都全量下发它）。

---

## 1. 实测账（2026-09-23，线上机器与生产库实测，不是估算）

| 项 | 实测值 |
| --- | --- |
| 服务器内存 | 总 **1866 MB**，可用 **777 MB**，**swap 已用 608 MB** |
| 服务常驻 | **55.8 MB RSS**（cgroup 43 MB） |
| 磁盘 | 40 GB，**19 GB 可用** |
| 生产库 | `battleship.db` **2.7 MB** + WAL 4.1 MB |
| `match_logs` | **293 行**；每局日志 **中位 6.6 KB**、最大 23 KB；**中位 35 步**、最多 117 步 |
| 行动类型 | `attack` 7706、`magic` 1734、`result` 184、`quick_chat` 87、`system` 7 |
| 规模 | `matches` **342 行**、`users` **34**、`user_profile` **8**；单人最多 204 局 |
| `matches` 的 id 形态 | uuid **484** / `ai-*` **176** / 游客 sid **24**（共 660 个座位） |
| 两边都是真实用户的对局 | **152 / 342** |

**结论：保留 30 局不用降，反而偏小。** 一条回放 ≈ 6.6 KB（日志）+ 1~2 KB（时间线）≈ **8~10 KB**。
30 局/人：今天全服所有回放约 **3 MB**；涨到 1000 活跃用户也只有 **~30 MB**，对着 19 GB 可用磁盘是噪声。

**真正的风险是内存不是存储**（可用 777 MB + 已在 swap）：

1. 回放**绝不进内存缓存** —— 不放模块级 dict、不加 `lru_cache`，只在"写"与"看"的瞬间各存在一份；
2. 记录器每局常驻内存 —— 契约原写"≤30 KB"，**实测做不到，已按实测修正**：
   真对局 20 局实测 **中位 92.6 KB / 最坏 153.4 KB**（落库那份 blob 中位 22.1 KB / 最坏 39.4 KB，
   约 229 B/步）。差 3~5 倍的原因是 CPython 里"每步一个 dict + text + detail dict"的固定开销，
   **不是实现冗余**（时间线已是稀疏+增量）。砍掉全部字段只省 8%，不值得，故**接受实测值**。
   量级判断：50 个并发房间 × 153 KB ≈ **7.5 MB**，对 777 MB 可用内存是噪声；
   真正会出事的是"回放留在内存里"，那条由 `_finalize_match` 的 `finally: replay.reset(room)` 保证。
   本机生产只有 34 个用户，真实并发房间远小于 50。
   `MAX_STEPS=2000 × 229 B/步 ≈ 458 KB`，与 512 KB 硬上限**自洽**（不存在"步数没超但 blob 超"的静默路径）；
3. 读的时候**只读一条**，并有 **512 KB 硬上限**：超限拒收并报警，绝不让坏 blob 打爆 1.8 GB 的机器。

另有一个必须同时改掉的缺口：战绩列表 `api.py:878` 默认 `limit=20`，存 30 局会有 10 局够不着。
前端战绩列表请求提到 **30**，代价是 `/user_stats` 单次响应 ~132 KB → ~200 KB（30 行 × 6.6 KB）。

---

## 2. 数据模型

```sql
CREATE TABLE IF NOT EXISTS match_replays
(
    match_id        TEXT PRIMARY KEY,
    winner_user_id  TEXT,            -- 真实 user_id；游客/人机座位为 NULL
    loser_user_id   TEXT,
    created_at      INTEGER NOT NULL,
    bytes           INTEGER NOT NULL,
    version         INTEGER NOT NULL,
    replay          TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_replay_winner ON match_replays (winner_user_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_replay_loser  ON match_replays (loser_user_id,  created_at DESC);
```

**为什么自带参与者两列**：实测 `matches.winner_id` 里混着 uuid / `ai-*` / 游客 sid，
长度上无法可靠区分 —— 从它反推"参与者是谁"是不可靠的。自带两列才谈得上"按人保留 30 局"与"参与者鉴权"。

`user_profile` 加 `allow_replay`：`_add_column_if_missing('user_profile', 'allow_replay', 'INTEGER DEFAULT 1')`，
配套 `ALLOW_REPLAY_DEFAULT = 1`，与 `allow_spectate` 同一套写法（教训 #14：老表加列只能判存在再加）。

---

## 3. 记录器（`replay.py`，纯模块）

**铁律**：`replay.py` **运行期不许 `import server`**（教训 #19）。

- **步**：`room.replay['steps']`，每步 `{i, kind, actor, text, detail}`；
- **船位时间线**：只在船位真变时记 `{step, p1:[{x,y,alive}], p2:[...]}`；
- **手牌时间线**：只在手牌真变时记 `{step, p1:[卡名], p2:[...]}`；
- **棋盘标记不存**：按步从 attack 步反推，再叠 `board_resets`（`{step, side}`）。

**喂数据只有两处，判据只有一份**：

1. `add_game_log(room, ...)` 末尾调 `replay.note(room, entry)` —— 一处覆盖现有 **38 个**行动记录点；
2. 当前**没有日志**的行动，在它们的**单一实现点**上调 `replay.note_action(room, kind, label, detail)`：
   `handle_end_turn` / `handle_enter_battle_phase` / `handle_enter_end_phase` /
   `handle_place_ships` 收尾 / `handle_rps_choice` / `handle_surrender`（约 6 处）。
   ⚠️ 这 6 处**不往游戏内日志加行** —— 游戏内日志的显示保持原样，不顺手改一个既有玩家可见功能。
3. **棋盘重置**复用第 5 批为观战加的 4 个失效点（`_mark_spectate_board_dirty`：
   `_clear_attacks_on_cells` / 灵气复苏 / 败者食尘 / 回光返照）—— **一处地方、两个消费方**。

**终局**：`replay.finalize(room)` 产出最终 JSON（`version` / 双方显示名 / `steps` / 两条时间线 /
`board_resets` / `nodes`）。**关键节点由服务端算一次**，前端不重新推导（两套判据必然漂移）。

**硬上限**：步数 ≤2000、blob ≤512 KB。触限就**截断 + 明确标注"此回放被截断"**，不静默。

---

## 4. 保留策略（每人最近 30 局）

写入点：`db.record_match(...)` 加 `replay=` / `replay_participants=` 参数，**同一个事务**里插
`match_replays`（`match_id` 由 `record_match` 内部生成，只有这样才能原子绑定，也才保证"有回放必有对局行"）。

**清扫（唯一实现，在 db.py）**，每次插入后对两个真实参与者分别做：

- 取该人 `matches` 按时间倒序**第 30 条**的时间戳 `cutoff(U)`；不足 30 条 → 不淘汰；
- 遍历该人**早于** `cutoff(U)` 的对局：对手不是真实用户 ⇒ 删；对手是真实用户但那一局仍在其窗口内
  ⇒ **留着**（共享的一条回放不能被先超窗的一方删掉）；两边都超窗 ⇒ 删；
- 每插入只枚举被淘汰的 1~2 条，不扫全表；
- **孤儿清扫**：`matches` 行被管理端回滚工具删掉时（教训 #28）对应回放一并清掉。

**不记录**：双方都不是真实用户（游客 sid 局，谁都读不到）；任一真实参与者 `allow_replay` 关着。

---

## 5. 权限与隐私

**开关**：`user_profile.allow_replay`，默认 **开**。UI 放设置面「隐私」区、
`#settings-allow-spectate` 旁边，状态**只来自服务端**（进设置面拉 `GET /api/replay/setting`），
保存失败必须提示 —— 照抄观战开关那一套。

**开关语义：任一方关掉 ⇒ 这一局就不留回放。** 只有这个语义真能保护关掉的人；否则你关了、
对手照样能反复研究你摆船。判定在 `_finalize_match` 收口处一次，新增
`_replay_allowed_for_match(winner_user_id, loser_user_id)`。

**读权限：能看你战绩的人都能看。** `GET /api/replay/<match_id>` 放行当且仅当：

- 请求者是**该局参与者**（`winner_user_id`/`loser_user_id` == 自己），**或**
- 该局某个真实参与者 `show_history = 1`（实测默认 **0 = 不公开** ⇒ 默认只有双方能看到）。

**假设 A1**：回放接口**要求登录**（游客 401）。观战已是"只限登录用户"，回放比观战敏感得多
（它泄露船位），不给匿名抓取留口子。代价：匿名浏览别人战绩能看列表、看不了回放。

**失败方向**：`get_allow_replay` 读库失败按**不开**处理（fail-closed，不记录）并**打日志报警**。
与观战相反（观战 fail-open），因为这里的错误方向是"泄露隐私"而不是"少个功能"。
**日志必须响** —— 吞掉等于没有守卫（教训 #34）。

**绝不做**：回放接口**不发任何 socket 事件**；`replay` 相关键**永不进 `SPECTATE_EVENTS`**；
`_build_spectate_snapshot` **永不读回放表**。三条都写成源码级守卫。

---

## 6. 后端接口

| 接口 | 作用 |
| --- | --- |
| `GET /api/replay/<match_id>` | 鉴权后返回 `{success, replay, you_are}`；`you_are` 服务端算好（`p1`/`p2`/`null`），**不下发任何 user_id** |
| `GET /api/replay/setting` | 读开关（状态只来自服务端） |
| `POST /api/replay/setting` | 写开关，`{'allow_replay': bool}`，非布尔 400 |
| `/user_stats`（改） | history 每行加 **`has_replay` 布尔**：`LEFT JOIN match_replays mr ON mr.match_id = m.id`，取 `mr.match_id IS NOT NULL`。**只取布尔，绝不取 blob** |

读接口内部：`MAX_REPLAY_BYTES` 校验 → `json.loads` → 校验 `version` → 返回。
**解析失败 ⇒ 明确报错 + 打日志 + 顺手删掉这条坏回放**，不静默返回空（教训 #32）。

---

## 7. 前端回放界面

**独立新屏 `#replay-screen`，不改造观战屏。** 硬决定：观战屏的棋盘渲染**故意**只会画"已轰过的格"，
其安全前提是"手上根本没有船位数据"；一旦让它学会画船，某次改动就可能把船位漏给观众。
所以新屏、新 DOM id（`#replay-board-1/2`），棋盘渲染**新写一份**，只共用 CSS 类
（`.cell/.hit/.miss/.sunk`）与 `magic_cards.js` 的卡名映射。

**入口**：`renderMatchDetailHTML(matchData, playerId)`（`game.js:4595`）加「对局回放」按钮
（带 `data-match-id`），`has_replay` 为假时置灰并写明"这局没有可回放的行动"。
点击用**事件委托**绑在 `#match-detail-content` 上 —— 容器每次重建，逐次绑监听必漏（大厅踩过同一个坑）。

**布局**（模仿观战屏骨架，去掉观战席与聊天）：

- 标题（双方名 + 胜负 + 时间）、顶栏（回合 / 阶段 / 场地魔法 / **速度** / 退出）
- 左右两块**全透视棋盘**（船格可见；命中 / 击沉 / 落空三态 + 船体）
- 侧栏：**双方手牌**（只读卡名）+ 对局日志 + 当前连锁（有则显示）
- 底部：`上一步` / `播放·暂停` / `下一步` / `退出` + **进度条**

**步进与状态解算**（客户端纯函数，一处实现）：

- 第 k 帧棋盘标记 = `steps[0..k]` 里的 attack 步，叠上 `board_resets` 中 `step ≤ k` 的重置；
- 船位 = 船位时间线中最后一个 `step ≤ k` 的条目；手牌同理；
- `上一步/下一步` = 改 `k` 后整体重画（≤2000 步直接重算，不需要 checkpoint）；
- **没有思考时间**：每步固定时长（默认 900 ms）÷ 倍速；档位 `1× / 2× / 4×`，`setTimeout` 链驱动，
  暂停即清定时器。

**进度条**：track + 按 `i/(n-1)` 绝对定位的节点标记，节点来自服务端 `nodes`（`{step, kind, label}`）。
**hover → tooltip** 显示那一步做了什么；**点击节点跳转**；**拖拽 track 任意位置 seek（并自动暂停）**。

---

## 8. 守卫清单（每条都必须证明"能红"）

1. **观战永不碰回放**（源码级）：`spectate.py` / `_build_spectate_snapshot` / `renderSpectateBoards`
   出现 `replay` / `match_replays` / 船位字段即判红；
2. **回放路径永不 emit**（源码级）：`replay.py` 与三个新接口出现 `emit(` / `socketio` 即判红 ——
   一旦漏，船位会直接进对局房间；
3. **观战不变量 1 复跑**：观战者每一帧仍无船位、无未公开坐标 + **自我校准腿**（注入假泄漏帧必须变红）；
4. **`/user_stats` 不含回放 blob**：30 局历史的响应字节数必须在上限内；
5. **鉴权三情形双向**：参与者可看 / 非参与者且 `show_history=0` 拒 / 非参与者且 `show_history=1` 放；
6. **开关语义**：任一方关 ⇒ **没有**回放行写入；双方都开 ⇒ 有（反向腿必须有）；
7. **保留 30**：第 31 局进来后最老那条被删；**共享回放**在一方超窗、另一方没超窗时**必须还在**；
8. **孤儿清扫**：删掉 `matches` 行后回放也被清；
9. **无内存缓存**（源码级）：`replay.py` 不许有模块级 dict、`get_match_replay` 不许挂 `lru_cache`；
10. **字节上限**：合成超限 blob → 拒收 + 明确报错（**不是**静默空）；
11. **记录点不漏**：38 个 `add_game_log` 点、6 个 `note_action` 点、4 个 `board_reset` 点逐个断言产生步骤；
12. **`allow_replay` 列迁移幂等**：重复 `init_db()` 不炸、老库能升级。

---

## 9. 边界与失败模式

| 情形 | 处理 |
| --- | --- |
| 某局 0 步（生产里**真有**这种行） | `has_replay=false`，按钮置灰写明原因 |
| 游客 sid 局（实测 24 个 id） | 不记录（谁都读不到） |
| 人机局（占战绩大头） | 记录；只有人类那侧的开关生效（AI 无设置） |
| 回光返照 / 灵气复苏 / 败者食尘重排棋盘 | `board_resets` 时间线；**不能**只靠 attack 反推（第 5 批那个 bug 的回放版） |
| 滥竽充数临时船回合末被收回 | 船位时间线必须跟着变 |
| 管理端回滚工具删了对局 | 孤儿回放被清扫 |
| 回放 JSON 解析失败 | 明确报错 + 日志 + 删掉这条坏回放 |
| blob 超 512 KB / 步数超 2000 | 拒收 / 截断并**明确标注**，绝不静默 |
| `Player` 缺失或设置读库失败 | 跳过该侧但**打日志**（fail-closed 方向见 §5） |

---

## 10. 明确假设

- **A1**：回放接口**要求登录**（§5 已说明取舍）。
- **A2**：`allow_replay` 默认 **开**（与 `allow_spectate` 一致）；但**默认外界看不到**
  （`show_history` 默认 0），所以上线不会一次性把所有人的对局公开。
- **A3**：保留 **30 局**（实测证明 30 完全放得下）；同时把前端战绩列表请求从 20 提到 **30**。
- **A4**：**没有 `match_logs` 的旧局不会凭空有回放**（实测 342 局里 49 局没有日志行）——
  历史局无法补录，只有本批上线后的新对局才有回放。公告与文档如实写，不假装能回放旧局。
- **A5**：回放**不含观战席与聊天**；但保留局内 `quick_chat` 作为步骤（它本来就是对局的一部分）。
- **A6**：回放**不给观众看、也不能被观战入口打开**；两个功能各有各的入口与通道。

---

## 11. 已知风险 / 不覆盖

1. **首帧时序**：`add_game_log` 在各 handler 里是"效果生效前"还是"后"调用，需逐点核对。
   若某处在前，该步的船位/手牌变化会**晚一步**显形。缓解：稀疏 diff 会累计到下一条记录，
   最坏是视觉差一步；用 E2E 逐帧比对钉住。
2. **6 个 `note_action` 点**落地时需确认是唯一实现点，避免漏记或重记。
3. **真机长局**：只在生产数据上算了体积，**没测过** 2000 步极端局的真实播放流畅度。
4. **多标签并发看回放**：不在本批验证范围。
5. **大回放对低端手机的渲染**：不覆盖（棋盘只有 36 格，风险低）。
6. **旧局补录**：不做（A4）。
7. 本批**不动**观战任何代码；若落地时发现必须动，先停下来说明再决定。

---

# 实施记录：前端 + 发布（2026-09-23 下午）

> 本节是**追加**的实施记录，上面 §0~§11 是**已写定的契约**，一个字都没改。
> 后端（`replay.py` / `db.py` / `api.py` 的回放段 / `server.py` 的接线）由另一批已完成并提交
> （`fa493b9` + `5e730bb`），本次只做**前端 + 一起发布**。

## R1. 交付范围（实际动了哪些文件）

| 文件 | 改动 |
| --- | --- |
| `templates/index.html` | 新屏 `#replay-screen`（独立一整屏）+ 设置面「允许保留我的对局回放」开关 |
| `static/game.js` | 回放引擎与渲染（顶层段）+ 模式标签唯一口径 + 列表 limit 20→30 + 入口委托 |
| `static/style.css` | `.replay-*` 一段（回放专用）+ `.hist-tag-*` 模式标签 + `.match-detail-actions` |
| `tools/replay_check.mjs` | **新建**：真浏览器 E2E（本批主证据） |
| `tests/test_replay_frontend.py` | **新建**：13 条前端源码级守卫 |
| `changelog.py` | 一条玩家公告 |
| `CLAUDE.md` | §0 / §5 / §12 / 末行（几十字级别） |
| `docs/REPLAY_2026_09_23.md` | 本节（**只追加**） |

**没有碰**：`replay.py`、`db.py`、`server.py`、`api.py`、`tests/test_replay*.py`、`tests/test_match_mode.py`。

## R2. 观战与回放的隔离（本批最重要的安全边界）

- 回放是**独立一整屏** `#replay-screen`，`#spectate-screen` 一个字都没改；
- 棋盘渲染**新写一份**：`renderReplayBoard` / `replayCellOf`，自己的 DOM id `#replay-board-1/2`，
  只共用 CSS 类（`.cell`/`.hit`/`.miss`/`.sunk`）与 `magic_cards.js` 的卡名映射；
- 回放段**完全不碰 socket**：不 join 房间、不发任何事件、不订阅观战通道；段内**没有一处 `emit(`**。
  那个 `replayState.socket` 字段只在退出时 `disconnect()`；
- 回放屏**没有观战席、没有聊天输入框**（契约 A5）。

两条源码级守卫（去掉注释后扫关键词，**都证明过能红**）：

| 守卫 | 判据 | 能红的改法 | 结果 |
| --- | --- | --- | --- |
| 观战渲染不吃回放 | `renderSpectateBoards` 函数体里不许有 `replay` / `match_replays` / `alive` | 在开头加 `const leak = gameState.replayPayload.ships` + `leak.alive` | `FAILED ... AssertionError: 观战棋盘渲染里出现了 'replay'` |
| 回放渲染不碰观战 | ① 回放段里不许有 `spectate*` 名字；② `renderReplayBoard` 里不许有 `renderSpectateBoards` / `applySpectateSnapshot` | 在 `renderReplayBoard` 开头加 `renderSpectateBoards(); void applySpectateSnapshot;` | `FAILED ... 回放段里出现了观战那套的名字：['renderSpectateBoards', 'applySpectateSnapshot']`（两条用例同时红） |

⚠️ **判据必须"去掉注释"再扫**：本段头部的说明注释里当然解释过"不许出现 spectate"，
那是文档不是耦合；而且这几条说明被**故意放在函数体之外**，函数体里连注释都不留这些词
（否则守卫会被自己的说明书误伤）。`ship` 这个词**允许**出现在观战渲染里 ——
那几处是 `mark.ship_sunk`（服务端告诉观众的**唯一**信息），不许据此推断旁边还有没有船。

## R3. `board_resets.side` 的方向 + 增量时间线的口径（照契约实现，未遇到歧义）

- `board_resets[].side` = **哪一块棋盘被重置**（`replay.py:399` 的 `board_reset(room, board_owner_id)`
  里 `_side_label(room, pid)` 就是被重置那位），所以重置只清**那一侧**的攻击标记；
  `回光返照` 传 `caster_id`、`灵气复苏` / `败者食尘` 不传（= 双方），与 `test_replay_guards.py` 一致。
- 两条时间线是**增量**（某条只带变化的那一侧）⇒ 前端按 `step` 升序**把所有 `step ≤ k` 的条目叠上去**
  （`replayFoldTimeline`），不是"取最后一条"。实测一条真回放的 `ships` 时间线：
  `step=0` 带 `p1+p2`，后面 6 条**只带 p1**（AI 那一侧的船被逐艘打沉），
  所以 p2 的船位必须从 `step=0` 那条一直沿用 —— 这正是"增量"最容易写错的地方。
- `detail.attacker` 是**原始座位 key**（实测就是 uuid user_id / 人机房里是入座 sid），
  **拿来当身份用会错**。前端判"这一炮画到哪块棋盘"的顺序：
  ① `detail.attacker === 'p1' || 'p2'`（通用）；② 退回按 `step.actor`（显示名）对齐座位名；
  ③ 两者都认不出**就不画这一炮**（宁缺勿错 —— 画错一侧比少画一炮更难查）。
  本次实测的 AI 房走的是 ②（`detail.attacker` 是 uuid）。

## R4. 后端契约让人"卡一下"的三个地方（都不是 bug，但都得知道）

1. **第 0 帧双方手牌本来就是空的**。`hands` 时间线的第一条（`step=0`）是 `p1: [] / p2: []`，
   真正的第一份手牌在 `step=1`（猜拳之后才发牌：先手 1 张、后手 2 张）。
   本批的 E2E 第一次就是这么**假红**的（"双方手牌不可见"）—— 其实工具读的是第 0 帧。
   判据改成"走到最后一帧，画面上的牌与后端最后一份手牌逐张相同"后才对。
   **给下一个人的话**：别把"开局那一刻的空手"当成渲染失败。
2. **阶段类节点的 `label` 自带行动者名字**（`note_action` 的 label 是 `_log_name(...)` 拼的），
   所以 tooltip 无脑拼 `actor + label` 会得到「第 3 步 · 甲 · 甲 进入战斗阶段」（实测读出来过）。
   修法：**标签里已经有这个名字就不补**（`replayNodeTipText`）。
3. **`steps[].sides` 在真数据里几乎不出现**：它是快照变化时用的（`build()` 把录制期的 `_side` 换成 `sides`），
   AI 房这一局的 10 步里一条都没有 —— 所以"这一步是谁做的"只能靠 `actor` / `detail.attacker`，
   不能指望 `sides`。

**没有发现后端 bug。**

## R5. 前端新东西清单（给下一个人）

**新 DOM id**：`#replay-screen`、`#replay-players-title`、`#replay-status`、`#replay-source`、
`#replay-step-index`、`#replay-step-total`、`#replay-step-kind`、`#replay-step-actor`、
`#replay-truncated`、`#replay-speed-group`、`#replay-name-1/2`、`#replay-ships-1/2`、
`#replay-board-1/2`、`#replay-board-title-1/2`、`#replay-hand-1/2`、`#replay-hand-title-1/2`、
`#replay-current`、`#replay-logs`、`#replay-prev`、`#replay-play`、`#replay-next`、
`#replay-track`、`#replay-track-fill`、`#replay-track-nodes`、`#replay-track-thumb`、
`#replay-node-tip`、`#replay-leave`、`#settings-allow-replay`、`#settings-replay-msg`。

**新函数**（全在 `game.js` 的 `REPLAY 界面段`，顶层作用域）：
`replayIsActive` / `replaySeatOf` / `replayInt` / `replayCardOf` / `replayModeLabel` /
`replayAttackerSeat` / `replayComputeFrame` / `replayFoldTimeline` / `replayCellOf` /
`replayAliveShips` / `replayNodePercent` / `replayNodeTipText` / `replayStepKindText` /
`renderReplayBoards` / `renderReplayBoard` / `renderReplayPlayers` / `renderReplayHands` /
`renderReplayCurrentStep` / `renderReplayLogs` / `updateReplayChrome` / `updateReplayTrack` /
`buildReplayTrackNodes` / `updateReplayNodeTip` / `hideReplayNodeTip` / `renderReplayFrame` /
`replayStepTo` / `replayStopTimer` / `replayScheduleNext` / `replayPlay` / `replayPause` /
`replayTogglePlay` / `replaySetSpeed` / `replaySeekFromPointer` / `resetReplayState` /
`replaySetStatus` / `leaveReplayScreen` / `replayHandleLoadFailure` / `openReplayForMatch` /
`bindReplayKeys` / `bindReplayControls` / `setReplaySettingMsg` / `loadReplaySetting` /
`saveReplaySetting` / `bindReplayEntryPoints`。

**模式标签（任务 B）唯一口径**：`matchModeLabel(matchData, myId)`（在 `bindEventListeners` 里）
→ `matchModeTagHTML(...)`；战绩列表 `buildHistoryList` 与详情页 `renderMatchDetailHTML`
**都调它**（原来那两处是各写一份、且只有"人机"一档）。回放屏经 `replayModeLabel` 复用同一份。

**其它**：`STATS_PAGE_SIZE` 20 → **30**（任务 C；`logs` 一个字没动）；
新增 `window.__bindHistoryForCheck(history, myId)` —— **只给无头工具用**（见 R7）。

## R6. 守卫清单（§8 的前端部分）逐条对照

| §8 条目 | 本批的守卫 | 能红的改法（原始输出见提交说明与本节 R2/R6） |
| --- | --- | --- |
| 1 观战永不碰回放 | `test_spectate_board_renderer_never_mentions_replay_or_ship_positions` | 让观战渲染读 `gameState.replayPayload.ships` → 红 |
| — 回放永不碰观战（本批新增） | `test_replay_section_never_mentions_spectate_rendering_or_socket` + `test_replay_board_renderer_is_not_the_spectate_one` | 回放渲染改调 `renderSpectateBoards()` → 两条同时红 |
| — `mode` 的 NULL 不许兜底 | `test_match_mode_never_falls_back_to_casual` + `test_unknown_mode_returns_empty_label_not_a_mode` | 写成 `mode \|\| 'casual'` → 红 |
| — 老局人机标签不许消失 | `test_ai_label_still_shown_for_legacy_rows_without_mode` + `test_is_ai_opponent_still_exists_and_still_used_by_mode_mapper` | 删掉 `isAiOpponent` 那条兜底 → 红 |
| — `has_replay` 为假 ⇒ 按钮不可点 + 原因文案 | `test_replay_button_disabled_with_reason_when_has_replay_is_false` | `canReplay = !!matchId` → 红 |
| — 入口必须是事件委托 | `test_replay_entry_is_bound_by_delegation_on_a_container_that_is_not_rebuilt` | 绑到 `#match-detail-content` → 红 |
| — 列表请求 30 行 | `test_stats_page_size_is_thirty` | 改回 20 → 红 |
| — 进出通道 + 固定速度 | `test_replay_screen_is_registered_and_has_an_exit` / `test_replay_timer_is_a_timeout_chain_not_an_interval` / `test_replay_progress_nodes_come_from_the_server_only` | 用 `setInterval` / 前端自算节点 → 红 |

## R7. E2E 工具踩到的坑（**工具自身**的，不是产品的）

`tools/replay_check.mjs` 从"跑不起来"到全绿，红了 6 轮。**每一轮都是工具/判据的问题**：

1. **`place_ships` 的每艘船必须带 `hits`**：服务端 `PlayerShip(**x)` 直接展开 dict，
   少了 `hits` 就是 `TypeError: missing 1 required positional argument: 'hits'`
   ⇒ handler 抛异常 ⇒ **连 ack 都不回** ⇒ 工具侧只看到 `timeout Runtime.evaluate`
   （正是 CLAUDE.md §1 那条"ack 帧没有长度位、handler 抛异常连 ack 都不回"）。
   当时是靠**看服务端日志的 traceback** 才定位的 —— 别先怀疑网络。
2. **猜拳会平局**（AI 随机出拳，1/3）：只发一手，撞上平局房间就永远停在 `rock_paper_scissors`。
   工具改成**循环出拳直到进入 attacking**。
3. **回合不是一次调用就换人**：一轮要走
   `enter_battle_phase` → 打光次数 → `enter_end_phase` → `end_turn`。
   只调前两步的症状是"404 炮打了 150 秒、`attacks_remaining` 一动不动"。
4. **目标集合必须本地记住"试过的格"**：服务端**拒掉的**那一炮不会出现在
   `test_get_game_state` 的 `attacks` 里，只看服务端状态就会反复挑同一个格子 ⇒ 活锁。
5. **不能把次数浪费在空海上**：不筛"还没沉的船"就会 30 炮 / 7 回合还赢不了（AI 把我方船打光、
   攻击次数掉到 3）。筛了之后 6 炮收工。
6. **我方摆船要用固定间隔的 6 格**：随机摆放偶尔会把 6 艘挤在一起，AI 几炮打掉一半 ⇒ 同 5。

**产品侧的两个真问题也是这轮翻出来的**（都不报错）：
- **战绩行的点击是 `bindHistoryButtons` 绑的**，工具直接把 HTML 塞进容器就绕开了它 ⇒
  点了没反应。为此加了 `window.__bindHistoryForCheck`（**只为工具存在**）。
- **进回放屏时旧浮层没关**：`#user-stats-modal` 与 `#match-detail-modal` 还挂着，
  于是"进了回放屏却还看着战绩弹窗" —— **是截图抓出来的**（截图里是战绩弹窗，不是回放屏）。
  修法：`openReplayForMatch` 调 `closeOverlaysExcept(null)`（按类名取全量 `.overlay`，不手抄 id）。
  ⚠️ 这条如果不修，玩家从战绩点进回放就**看不到回放**，而且页面上不报任何错。

## R8. E2E 实测数字

| 项 | 值 |
| --- | --- |
| 局 | 人机（easy）真打完：6 炮 / 0~1 次交回合 / 2~6 秒 |
| 后端 | `steps` 10~17、`nodes` 10~17、`board_resets` 0、`truncated` null |
| 船位 | 后端时间线 `p1=6 / p2=6` 格；画面最后一帧 **画了 6 / 6 格**（逐侧相等） |
| 手牌 | 后端 `p1=["失灵！","极限增援"] p2=["看破！"]`；画面逐张相同（卡名都在 `magic_cards.js` 里） |
| 步进 | 连点 6 次下一步 ⇒ k=6；上一步 ⇒ 与前一帧**逐格相同** |
| 拖拽 seek | 拖到 k=N 与"连点 N 次下一步"的**两块棋盘逐格相同**（核心判据） |
| 倍速 | 1× 首步 **937~943 ms**、4× 首步 **236~251 ms**（实测间隔，不是只看 class） |
| 退出 | 回开始界面 + `replayState.payload` 被清空 |

## R9. 四个工具 + E2E：跑了几次 / 几次绿

| 工具 | 次数 | 结果 |
| --- | --- | --- |
| `node --check static/game.js` / `tools/replay_check.mjs` | 每次改动后各 ≥6 次 | 全绿 |
| `node tools/dom_contract_check.mjs` | 3 次 | **3/3 绿**（"DOM 契约对账全部通过"） |
| `node tools/ui_layout_check.mjs --url http://127.0.0.1:5099/` | 1 次 | **1/1 绿**（逐视口全通过，含 390/664/768/1600） |
| `node tools/spectate_check.mjs --url http://127.0.0.1:5099/` | **1 次** | **1/1 绿**（含自我校准腿"注入假泄漏帧必须红" → 实测 `badCount:4`，判据真的能抓） |
| `node tools/replay_check.mjs` | 7 次（其中 6 次是**工具自己的 bug**，见 R7） | 最后 **2/2 绿**（含加了浮层断言之后的那次） |
| `python -m pytest tests/ -q` | 3 次（基线 / 加守卫后 / 发布前） | **2395 passed**（基线 2382，只多不少） |

那 6 次红的分别是 R7 的 1~6 条，逐条都定位到了具体原因（不是"重试就好了"）。

## R10. 公告与文档

公告加在 `changelog.py` 最上面，`date = 2026-09-23 13:57`（真实时间，与上面那条 10:49 严格降序）：

- `新增了：战绩里可以点开一局回看全过程，双方船位与手牌都看得到`
- `新增了：战绩列表和对局详情会标明这局是排位、匹配、人机还是自定义房`

⚠️ **公告**没有**、也**不许**承诺"能回放旧局"** —— 契约 A4：实测生产库 342 局里 49 局
根本没有日志行，历史局**无法补录**，只有本批上线后的新对局才有回放。这一条也如实写在这里。

## R11. 仍未验证 / 不覆盖（诚实清单）

1. **30 行的战绩列表响应体积**：契约 §1 说会从 ~132 KB 涨到 ~200 KB。
   本批**没有实测**这个数字（本地库只有 1 局），只改了 `STATS_PAGE_SIZE` 一个常量。
2. **排位 / 匹配 / 自定义房三种 `mode` 标签**：只在**人机局**上端到端验过（`mode='ai'`）。
   `ranked` / `casual` / `custom` 三条分支只有源码级守卫，**没有真跑过一局**。
3. **`mode` 为 NULL 的老局在真数据下的显示**：生产库里 342 行老局全是 NULL，
   但本批**没有**对着生产数据实跑（本地库是空的）。源码级守卫
   `test_match_mode_never_falls_back_to_casual` 钉住了"不许兜底成匹配"。
   人机标签的"老局退回 `isAiOpponent`"这条腿也**只有源码级断言**，没有真老局数据。
4. **`board_resets` 非空的回放**：本批打的局 `board_resets` 都是 `[]`，
   所以"回光返照 / 灵气复苏 / 败者食尘之后棋盘被擦干净"这条**没有端到端验过**
   （只有后端 `test_replay_guards.py` 的源码级守卫与前端 `resetAt` 逻辑）。
5. **401 / 403 / 409 / 500 四种失败提示**：代码里逐条分了文案
   （`replayHandleLoadFailure`），但 E2E **只验到了 200**。
   404 的置灰路径验过（`has_replay` 为假 ⇒ 按钮禁用 + 写明原因），
   **四种 HTTP 失败没有实跑**。
6. **2000 步 / 截断回放**：`truncated` 的横幅与节点是照契约写的，
   但**没有跑过**截断或超长回放（契约 §11.3 本来就标了不覆盖）。
7. **多标签并发看回放**：不覆盖（契约 §11.4）。
8. **低端手机 / 真机触摸拖拽**：拖拽是用 CDP 派发的 PointerEvent 验的
   （`pointerdown/move/up` + 逐格比对），**没有真手指/真机验证**。
9. **回放开关（`allow_replay`）的 UI 保存链路**：开关与提示文案是照观战那套写的，
   但 **E2E 没有实跑**（本批只跑通了"能开回放"这条路，没验"关掉之后不留回放"）。
   后端有 `tests/test_replay_*.py` 的开关语义用例，前端这条**没验**。
10. **线上实测**：本机服务与工具全绿；线上只验"服务 active + 健康检查 + `/api/changelog` 能看到新公告"，
    **没有在线上点开一局回放**（线上生产库的老局都没有回放，需要有新对局）。

---

# 实施记录：作者实报的 4 条回放缺陷（2026-09-23 傍晚）

> 本节同样是**追加**的实施记录。上面 §0~§11 是**已写定的契约**、R1~R11 是上一批的实施记录，
> **都没有改动一个字**。本批只修缺陷，不改契约口径（`effects` 那一条见 D3 的说明：
> 它是 §7"第 k 帧棋盘标记"那一段的**补充**，不是新契约）。

作者原话：
> 回放功能还存在很多问题 比如说船存在的格子显示错误会出现一个叉叉 看不到双方已经打了的格子的标记
> 也看不到特殊效果发生的格子比如说仁王之盾的给盾或神威的挖格子等等

## D1. 缺陷 ①：船格画成了 `✕`（前端，字形口径）

**根因**（`static/game.js` 的 `renderReplayBoard`）：

```js
el.textContent = (item.cell.hasShip || item.cell.hit) ? '✕' : (item.cell.miss ? '○' : '');
```

`✕` 是**命中**的字形，而这一行把它给了**任何有船的格子** ⇒ 玩家看到的是一整片红叉，
"这一格已经打过、别再点了"——而作者看的是回放，本来就没有"能不能打"这回事。
上面那几行的 **class 是对的**（`ship` / `ship hit` / `ship sunk hit`），坏的**只有字形**。

**改法**：三个字形各管一个含义，互不重复。

| 状态 | 字形 | class |
| --- | --- | --- |
| 有船、没挨过炮 | `⛴` | `cell ship` |
| 挨过炮（命中） | `✕` | `cell hit` / `cell ship hit` |
| 击沉 | `✕` | `cell ship sunk hit` |
| 落空 | `○` | `cell miss` |
| 空海 | （空） | `cell replay-water` |

**为什么选 `⛴`**：船在**其它屏**上本来就不是靠字符表达的（`.cell.ship` 是不透明金属底
+ 立体阴影，没有字形），所以回放必须**新选**一个符号；`⛴` 在本项目里没有被别处占用
（`❄` / `⛨` / `?` / `✕` / `○` / `🛡` 都已各有含义）。`title` / `aria-label` 同口径写明
「战舰：这一格有船，还没被打到」。

**改前 → 改后（真实数据，`tools/dom_replay_frame_check.mjs` 逐格读出来的）**：

```
改前：未挨炮的船格 (2,2) -> {"text":"✕","cls":"cell ship"}        ← 作者看到的叉叉
改后：未挨炮的船格 (2,2) -> {"text":"⛴","cls":"cell ship","title":"战舰：这一格有船，还没被打到"}
```

## D2. 缺陷 ②：双方已打过的格子一个都画不出来（前端，**方向写反**）

**根因**（`static/game.js` 的 `replayComputeFrame` 第 ③ 段）：

```js
var after = false;
var marks = frame.resetAt[boardSide];
for (var m = 0; m < marks.length; m++) {
    if (marks[m] <= i) { after = true; break; }
}
if (!after) continue;                       // ★ 正常的局在这里全军覆没
```

语义被写成了「**必须存在**一个 step ≤ i 的重置，这一炮才画」。而绝大多数局的
`board_resets` 是**空数组**（本批实测：人机局 `board_resets=[]`）⇒ `after` 恒为 `false`
⇒ **每一炮都被 `continue` 掉** ⇒ 一块棋盘上一个"已轰过的格"都没有。作者报的第二条正是它。

**正确语义**（按任务给的口径改，没有自创）：一炮（step i，打 board B）在帧 k 要看得见，
当且仅当它发生在 **B 的最后一次重置之后**——重置会把那块棋盘上已有的标记全擦掉。
等价写法就是取该数组的**最大值** `lastReset`（只收 `step ≤ k` 的重置；没有则 `-1`）：

```js
if (i <= lastReset[boardSide]) continue;      // 重置把它擦掉了
```

⚠️ **为什么上一批全绿**：既有断言只有两条腿 ——
①「拖拽 seek 与连点下一步**自洽**」（系统性丢标记**也自洽**）；
②「船格 6/6 与后端 JSON 相等」（**船 ≠ 攻击标记**）。
**没有任何一条断言要求攻击标记真的出现过**。这正是教训 #7（恒等式断言拦不住方向写反）
的同一形状：不是判据算错了，而是**根本没有那条判据**。

**改前 → 改后（真实数据，`tools/replay_check.mjs` 真打完一局读出来的）**：

```
改前：帧里的标记表 frame.marks: p1=[] / p2=[]
      后端推出的标记键:        p1=["0,0","0,5","1,1","2,1","3,0","5,0"] / p2=[]
改后：帧里的标记表 frame.marks: p1=["0,0","0,5","1,1","2,1","3,0","5,0"] / p2=[]
      → 与后端推出来的集合**逐格相等**
```

⚠️⚠️ **这条判据有一个"看着像绿、其实是假绿"的陷阱，必须写下来**：
`replayCellOf` 的 `sunk` 那一支**自己就会给格子加 `hit` 类**
（`sunk = shipCell.sunk && alive !== true` ⇒ 画成 `ship sunk hit`）。而真打完的局里
最后 6 格**全是被我们打沉的敌舰** ⇒「画面上有 6 个 hit 格」这件事在**修好与没修好时都成立**。
本批第一版判据就是这么写的，结果"改坏 → 仍然绿"，差点被误判成"判据抓不住 bug"。
⇒ 现在的判据分两条**互相独立**的腿：① **帧里的标记表**（`replayState.frame.marks`，
这才是缺陷 ② 控制的东西）与后端推出来的集合逐格相等；② 画面上的 `hit`/`miss` 格
== 帧里的标记 **∪** 后端 `ships` 时间线里独立推出来的沉船格。

## D3. 缺陷 ③：特殊效果格看不到（后端加一条时间线 + 前端画）

**实测结论：后端原来**根本没有这些字段**。**`grep -n 'shenwei|frozen|last_stand|shield|effect' replay.py`
只命中一处"攻击步剥字段"的注释。所以前端无从画起——不是渲染漏了，是**数据不存在**。

**数据源（grep 出来的真相，照抄观战那一份口径，教训 #1）**：

| 效果 | 真实来源 | 形状 |
| --- | --- | --- |
| 神威挖的洞 | `room.game_effects['shenwei_holes']` | `[{'player': <pid>, 'x1','y1','x2','y2','return_turn'}]` |
| 冻结区 | `room.game_effects['frozen_area']` | `{'x1','y1','x2','y2','owner': <受害者的 pid>, 'caster', 'frozen', 'until_round'}` |
| 绝处逢生候选格 | `room.game_effects['last_stand_cells']` + `['last_stand_owner']` | `[(x, y), ...]` + caster pid |
| **仁王之盾 / 卧薪尝胆的护盾** | **`PlayerShip.shield`（不在 `game_effects` 里！）** | 逐艘 `ship.shield = True` |

⭐ 护盾那一行是本批**必须 grep 才敢动手**的一条：`grep -n '\.shield' server.py` 显示
`server.py` 的'仁王之盾'分支与'卧薪尝胆'分支都是**逐艘 `ship.shield = True`**，
`room.game_effects` 里**没有**护盾这个东西。任务里已经点了这一条，
所以这里如实记下：**护盾读的是船对象，不是 `game_effects`。**

**新增数据形状**（稀疏 + 增量，与 `ships` / `hands` 完全同一种套路）：

```jsonc
"effects": [ {"step": 0, "p1": {…五个字段…}},        // 只带**变了的那一侧**
             {"step": 2, "p2": {…}} ]
```

五个字段名**逐个照抄** `_build_spectate_snapshot` 的公开效果段：
`shield` / `shenwei_holes` / `frozen_area` / `last_stand_cells` / `last_stand_owner`。
`shield` 与 `last_stand_cells` 都是**坐标对** `[[x, y], ...]`（与观战那一份同形状）。

⚠️ **归属一律转成座位代号**：`frozen_area.owner` 与 `last_stand_owner` 在
`game_effects` 里是**原始 pid**（匹配房里就是入座 sid），照原样放进回放 JSON 等于
把"谁是谁"送出去（契约 §6：响应里不许有 user_id）。
—— 这是**实测抓到的**：`test_effects_never_leak_a_raw_pid` 第一版就是红的。

⚠️ **两条都踩过的坑（都写进了代码注释）**：
① **"变成空"必须记**（盾被打掉之后前端要靠这条空表把盾撤掉）；
② **"从头到尾都空"不许记**（绝大多数局一个效果都没有，"每局一条空行"纯属白占体积）。
判据必须同时看**上一次**状态，而 `bool(效果dict)` **恒为真**（键一个不少、值全空）
——本批第一版就是用 `any(bool(v))` 写的，恒成立、等于没有判据（教训 #34 同族）。

**读接口**：`GET /api/replay/<id>` 多下发一个 `effects` 数组（**只在 `replay` 里面**）。
观战通道一个字都没动（`replay` 相关键仍然永不进 `SPECTATE_EVENTS`，见 §5 与守卫 1/2）。

## D4. 缺陷 ④：`（你）`标到了对手身上 —— **是后端 `you_are` 算错，不是前端交叉**

**先给结论与证据**（两件事分开判，修法完全不同）：

- **前端没有交叉。** `replaySeatOf(i)` 是 `i === 1 ? 'p2' : 'p1'`（槽位 0→p1、1→p2，
  与 `names = [p1_name, p2_name]` 的数组下标**同序**）。`tools/dom_replay_frame_check.mjs`
  的 E 组把这条钉住了：造 `p1_name='AI'` / `p2_name='小小弈11'`、`you_are='p2'` 的场景，
  断言「槽位 i 的显示名 == 座位 `replaySeatOf(i)` 的名字」——改前就是绿的。
- **后端 `you_are` 算错了。** `replay.py` 原来的判据是
  `winner_user_id == viewer_uid ⇒ 'p1'`、`loser ⇒ 'p2'`，而 `p1` / `p2` 是
  **`room.players` 的插入顺序**（= 入座顺序），**与胜负毫无关系**。
  它自己的 docstring 甚至写着「录制时的先手 `p1` 不一定是胜者」——注释是对的、代码是反的。

**E2E 实证（`tools/replay_check.mjs` 真打完一局）**：

```
座位对齐: {"t1":"AI 的棋盘","t2":"rplchk_… 的棋盘","mine1":false,"mine2":true,
           "n1":"AI","n2":"rplchk_…（你）"} / you_are="p2" / p1_name="AI" p2_name="rplchk_…"
```

人机局里**人类坐在 p2 并且赢了** —— 旧口径会返回 `'p1'`，于是 `（你）` 落到 `p1_name`
（= AI）那一侧：**与作者截图里 `AI（你）` 的现象完全对应**。

**修法**：`replay.build()` 把 `seats = {uid: 'p1'|'p2'}`（按 `room.players` 插入顺序）
一起打进 blob；`you_are(payload, row, viewer_uid)` 改成**先查 `seats`**。
老 blob（本批之前落的）没有 `seats` ⇒ 退回旧口径，但**只对"确实在参与者两列里"的人**兜底
（旁观者一律 `None`，不许把"未知"退化成"满足条件"，教训 #21）。

**为什么上一批的单测全绿**：`tests/test_replay_api.py::test_participant_can_read_and_gets_you_are`
的夹具里**恰好** `winner == p1`（对称局面测不出方向）——又是教训 #7。

### D4b. ★★★ 生产库里的直接证据（本批实测，只读）

上服务器读 `/opt/battleship/data/battleship.db`（**只读**）里的 `match_replays`，
生产库里**恰好只有 1 条回放**，而它就是**作者截图里的那一局**：

```
match_id = d0da931a-8ad7-457b-b6b5-b029798620d2   bytes=15759  version=1
blob 里的键 = ['board_resets','hands','nodes','p1_name','p2_name','started_at',
               'steps','ships','truncated','version']      ← **没有 seats / effects**
p1_name='AI'   p2_name='小小弈11'   winner='小小弈11'   loser=None   seats=None
steps=60  ships=7  hands=12  effects=0  board_resets=0
```

三重结论：

1. **`p1` = AI、`p2` = 作者**，而**赢的是作者**（`winner='小小弈11'`）。
   旧口径 `you_are` 的第一条就是"胜者 ⇒ p1" ⇒ 它会返回 **`'p1'`**
   ⇒ 前端把 `（你）` 标到 `p1_name`（= **AI**）那一侧 ——
   **与作者截图里的 `AI（你）` 逐字对应**。这不是推测，是这一条生产数据的必然结果。
   （`loser=None` 因为那是人机局的 AI 坐席，所以"败者是谁"这一侧本来就没有 uid；
   旧口径下"赢的人被当成 p1"这条**不需要**知道败者就成立。）
2. 这条 blob **没有 `seats`** ⇒ 它是**本批之前**落的，走的是"老回放兜底"那条路
   （修好之后它仍然会被退回旧口径 —— 见 D10 第 7 条）。
3. 它 **`board_resets=0`、`effects=0`、60 步、15.4 KB** —— 正是缺陷 ② 与 ③ 的现场：
   60 步里一炮都没少记，而回放界面上**一个标记都画不出来**。
   （这条 blob 也顺带说明：上一批"`board_resets` 都是 `[]`"的观察在生产数据上同样成立。）

⚠️ 本项只有**一条**样本（生产库里就这一条回放），所以它是"这一个案"的实证，
不是"生产数据里 p1 一定是败者"的统计结论 —— 后者本批**没有**量（见 D10）。

⚠️ **额外的好消息**：本批实测发现人机局里**人类一贯是 p2、AI 是 p1**
（`p1_name="AI"`，见上面那行真实输出）。所以作者看到的 `（你）` 挂在 AI 上，
正是这条 bug 的**必然**表现，而不是偶发。

## D5. 本批新增/扩展的守卫（每条都证明过能红）

| 守卫 | 位置 | 改坏法 | 结果 |
| --- | --- | --- | --- |
| ★★ 攻击标记必须出现过（A 组 4 条） | `tools/dom_replay_frame_check.mjs` | 把第 ③ 段改回 `after=false` 写法 | **红 14 条**（`{"dom":"[]"}` vs 后端 6 格） |
| ★★ 重置语义双向（B 组） | 同上 | 同上 / 重置方向反了 | 红 2 条 / 红 13 条 |
| ★ 一块棋盘重置两次只留最后一次 | 同上（**专门为"取 max"写的**） | 把 `> lastReset` 改成 `<`（取最早那次） | **红 1 条**：`dom=["2,5","4,5"]` 期望 `["4,5"]` |
| ★★ 船格字形（C 组） | 同上 | `textContent = (hasShip \|\| hit) ? '✕' : …` | **红 5 条** |
| ★★ 效果格 + 随时间线变化（D 组） | 同上 | 删掉 effect 的四个 class 分支 / 不叠 `effects` 时间线 | 红 7 条 / 红 10 条 |
| ★★ 槽位 / 座位对齐 + `（你）`（E 组） | 同上 | 把 `names` 的两个键对调 | **红 4 条** |
| ★★ 帧里的标记表 == 后端推出来的集合 | `tools/replay_check.mjs`（真 E2E） | 同上（缺陷 ② 的原写法） | **红 1 条**：`frame={p1:[],p2:[]}` vs `server={p1:[6 格]}` |
| 效果时间线（20 条） | `tests/test_replay_effects_and_seats.py` | —— | 见文件内逐条说明 |
| `you_are` 按座位算（7 条） | 同上 | 把 `you_are` 改回胜负口径 | 见文件内逐条说明 |
| 前端源码级 4 条（字形/标记/效果格/座位对齐） | `tests/test_replay_frontend.py` | 同上各自的改坏法 | 见文件内说明 |

`tools/dom_replay_frame_check.mjs`：**46 项全绿**（不用浏览器、不用服务端，几秒钟）。

## D6. 工具侧：本批最费时间的一个坑（**缓存**）

做"改坏源码 → 判据必须红"的红证时，**`tools/replay_check.mjs` 一连两次都假绿**：
工作树里的 `game.js` 明明已经改回缺陷写法（走 HTTP 取源码确认 `var after = false;` 在里面），
浏览器却照样全绿。**根因**：本工具用**持久 profile**（`.tmp/replay_check_profile`），
而 `index.html` 里的 `?v=<asset_v('game.js')>` 是**按 mtime（秒级）**取的 ——
同一秒里改坏再改回，URL 一模一样 ⇒ Chrome 直接命中磁盘缓存，跑的是**上一份**代码。

**修法（三层，缺一不可）**：`--disable-http-cache` + `--disk-cache-size=1` 启动开关、
CDP `Network.setCacheDisabled`、`Network.clearBrowserCache`；另外工具开头会**打印
"浏览器执行的那一份 game.js"**（含 `hasAfter` / `hasLastReset` 两个探针），
下一个人一眼就能看出这一轮跑的是哪一份。**这条值得记住**：
"改坏了判据却不红"要先怀疑**跑的根本不是那一份代码**，别急着下"判据抓不住"的结论。

## D7. 工具侧：`replay_check.mjs` 的两处脆性（都定位到了根因，不是"偶发"）

1. **后手那一局打不完**（实测同一份源码连跑 3 次，两次红）：
   `[poll 8..14] attacker=me attacks=6→1、oppShips 停在 1` ⇒ 150 秒没打完。
   根因是**规则本身**：*攻击次数 = 存活船数 − 冻结船数* —— 先手方每回合白打 6 发，
   后手方每回合先挨打（沉一艘 ⇒ 次数上限永久 −1），于是"6 艘追不满"。
   ⇒ 修法：**猜拳必须先手**；AI 拿到先手就放弃这一间房、重开一间（上限 5 次）。
   另外"搜海"改成按**棋盘奇偶**扫（6 艘单格船一定被两个奇偶类之一覆盖）。
2. **3 炮就赢的局**（某张卡直接结束对局）：回放里 `attack` 步为 0 ⇒
   "攻击标记必须出现过"这条**没有可断言的对象**。⇒ 工具会**明确失败并说明原因**
   （"请重跑一次工具"），绝不静默挑一局将就。
   ⚠️ 更早就踩过的同族问题：放弃的那几间房**也会被结算落库、各留一条回放**，
   于是"战绩里最新一行"可能不是我们真打完的那一局 ⇒ 现在按
   "回放里真的带 `attack` 步"来挑行。

## D8. 体积实测（本批：加了 effects 之后）

真打完 **10 局**（无头驱动器，与 pytest 同一套），量落库那一份 payload：

| 项 | 实测 |
| --- | --- |
| blob（含 `effects`） | 中位 **26.9 KB** / 最坏 **42.3 KB**（10 局） |
| 这些局的步数 | 50 / 81 / 82 / 100 / 106 / 134 / 137 / 139 / 160 / 177 |
| 契约 §1 的基线（上一批实测） | blob 中位 **22.1 KB** / 最坏 **39.4 KB** |
| **`effects` 带来的增量** | **中位 0 B / 最坏 0 B** |
| 这些局的 `effects` 行数 | **0 条**（自对弈的两侧都不打效果卡） |
| 【上界】最长那局（177 步）按"每 10 步一次、两侧都变"灌满 | 42.3 KB → **48.1 KB（+5.8 KB）**，仍远未触 512 KB |

⚠️ **对基线差异要诚实**：本批的中位 26.9 KB **不能**直接与上一批的 22.1 KB 相减当"涨了" ——
两批跑的是**不同的局**（本批有 177 步的长局）。同一个 payload **去掉 effects 再量一次**的差是
**0 B**，所以"effects 让 blob 变大"这件事在真实自对弈局里**等于零**，上界是 +5.8 KB。

## D9. 本批跑了几次 / 几次绿（**如实**，含早期那些我自己造成的红）

| 工具 | 次数 | 结果 |
| --- | --- | --- |
| `node --check static/game.js`（+ tools） | 每次改动后 | 全绿 |
| `node tools/dom_replay_frame_check.mjs` | **9 次** | **9/9 绿（46 项）**；另跑 6 个变体做红证，**6/6 都变红**、改回后全绿 |
| `node tools/replay_check.mjs`（真 E2E） | **22 次** | **13 绿 / 9 红**，9 次红逐条都定位到了原因（**没有一条**是"重试就好了"）：<br>· 3 次 **假绿**（改坏了却不红）→ 根因是浏览器磁盘缓存（见 D6，已修）<br>· 3 次 `150s 没打完` → 后手那一局追不上（见 D7-1，已修：必须先手）<br>· 2 次 `attack 步为 0` → 3 炮就赢的局（见 D7-2，已改成明确报错）<br>· 1 次 `wantB1 is not defined` → 我自己改动留下的悬空变量（改法：把判据换成 `wantKeys1/2`）<br>最后 3 次连跑（缓存开关 + 先手保证 + 挑对回放之后）**3/3 绿**，每局 6 炮 / 2 秒 |
| `node tools/dom_contract_check.mjs` | 1 次 | **1/1 绿** |
| `node tools/ui_layout_check.mjs` | 1 次 | **1/1 绿**（改动只加颜色/阴影/伪元素，没动盒模型） |
| `node tools/spectate_check.mjs` | 1 次 | **1/1 绿**（四浏览器 + 不变量 1 `badCount:0` + 自我校准腿 `badCount:4`） |
| `python -m pytest tests/ -q` | 3 次 | **2419 passed**（基线 2395，只多不少） |

## D10. 仍未验证（诚实清单）

1. **线上点开一局回放看效果格**：线上只有**新对局**才有回放，且要真打出护盾/神威/冻结
   才会出效果格。本批线上只验"服务 active + 健康检查 + `/api/changelog` 能看到新公告"。
2. **`effects` 的非空端到端**：真正打出"仁王之盾 / 神威 / 冻结 / 绝处逢生"的对局
   **没有在浏览器里端到端验过**（E2E 打的人机局两侧都没上效果）。非空形状只由
   `tools/dom_replay_frame_check.mjs`（喂手工 payload 给真 `replayComputeFrame`）
   与 `tests/test_replay_effects_and_seats.py`（喂真房间）钉住。
3. **护盾格与"盾被打掉"之后的那一帧**：后端有"记成空表"的用例，
   前端有"空表 ⇒ 不画盾"的逐格用例，**但两者之间那一跳（真对局里盾消失）没跑过**。
4. **`board_resets` 非空 + 效果的组合**：重置时间线与效果时间线**同时**非空的局没跑过
   （上一批 R11 也记了同一条；本批只在 vm 工具里分别造过）。
5. **多标签并发看回放、低端手机**：不覆盖（契约 §11.4/§11.5）。
6. **`effects` 在 2000 步极端局里的体积**：只按"每 10 步一次、两侧都变"做了上界估算
   （+5.8 KB），**没有**真造一局 2000 步的。
7. **老回放（本批之前落的、blob 里没有 `seats`）的 `（你）`**：代码里有兜底路径，
   只有单测覆盖，**没有拿生产库里的老回放实测过接口**。生产库里确实有 1 条这样的老回放
   （见 D4b，`seats=None`），但**没有以作者身份的登录态去请求 `/api/replay/<id>`**
   （拿不到作者的会话），所以"老回放走兜底之后界面长什么样"这件事**没验证**。
8. **生产数据的统计面**：D4b 的证据是**单条样本**（生产库里只有 1 条回放）。
   "生产数据里 `p1` 到底更常是胜者还是败者"**没有量**，也不该由这一条推。

---

# 实施记录：作者实报第三条 —— 主动牺牲的船在回放里"直接消失"（2026-09-23 深夜）

> 本节同样是**追加**的实施记录。§0~§11（契约）与 R1~R11、D1~D10（前两批的实施记录）
> **都没有改动一个字**。本批只修缺陷。

作者原话：
> 主动牺牲的船会在回放的棋盘中直接消失掉 而不是变成红色叉叉

## E1. 根因（后端，**不是前端**）

**唯一实现点是 `server._do_demon_contract_sacrifice`**（`demon_contract` / `divine_decree` /
`dice_sacrifice` / `trap_sacrifice` 四种 reason 全部汇到这里，grep 实证）：

```python
player.ships.remove(ship)          # ★ 船从列表里被摘掉
_mark_ship_sunken(player, ship)    # 登记进 sunken_ships（游戏自己是记得的）
...
positions = [{'x': p.x, 'y': p.y} for p in ship.positions]
add_game_log(room, ..., 'magic', {'player': player_id, 'reason': reason,
                                  'positions': positions})      # ← 坐标**本来就在**
emit('ship_sacrificed', {'player': ..., 'positions': positions, 'reason': ...}, room=room.id)
```

而船位时间线（`replay._ship_cells`）**只遍历 `player.ships`** ⇒ 船被摘掉之后那一格就不再进快照
⇒ 前端的**增量叠加**（`replayFoldTimeline` 按 `side` 整体替换）把那一侧数组换成不含它的版本
⇒ 回放棋盘上那格**凭空消失**。

**前端一个字都不用改，实测确认**：`replayCellOf` 的
`var sunk = !!(shipCell && shipCell.sunk && shipCell.alive !== true);` 本来就能画红叉
（`ship sunk hit` + `✕`），前端从来不是问题所在（D1 已经修过字形）。本批**没有碰 `static/game.js`**。

**改前 / 改后（真实数据，`.tmp/dbg_sacrifice.py` 走真实现点读出来的最后一帧）**：

```
改前: p1=[{'x': 3, 'y': 3, 'alive': True}]                       ← (2,2) 整格消失
改后: p1=[{'x': 3, 'y': 3, 'alive': True},
          {'x': 2, 'y': 2, 'alive': False, 'sunk': True}]        ← 沉没格留下了
前端读到的 DOM: (2,2) -> {"text":"✕","cls":"cell ship sunk hit","title":"击沉：这一格的战舰已被打沉"}
```

## E2. 改法：**显式事实**，不是"从表象反推"

三件套（口径与 `note_board_reset` 同一套路，都只改记录器自家状态、**绝不 emit**）：

| 入口 | 谁调 | 干什么 |
| --- | --- | --- |
| `replay.note_ship_lost(room, pid, positions)` | **只在主动牺牲的实现点** | 把这几格登记进"牺牲格"表；`_ship_cells` 把它们以 `alive:False` + `sunk:True` **留在**时间线里 |
| `replay.note_ship_returned(room, pid, ship)` | 疗愈 / 死者苏生 / 神机妙算重新部署 | 撤销该船**原位置**的登记（船回来了 ⇒ 红叉必须消失） |
| `replay.note_board_replaced(room, owner?)` | 灵气复苏 / 败者食尘 / 回光返照 | 旧棋盘整块换新 ⇒ 其上的牺牲格一起作废 |

⚠️ **数据来源单一**：登记用的就是 `_do_demon_contract_sacrifice` 手上那一份 `positions`，
**绝不去 diff 前后快照猜**"哪几格消失了" —— 那是从表象反推，会把滥竽充数收回的临时船
（卡面明写"不会显示沉没"）一起判成沉没。这条写进了 `_ship_cells` 的注释与一条源码级守卫
（扫 `note_ship_lost` 的**调用点所在函数**，白名单只有
`_do_demon_contract_sacrifice` + `apply_magic_effect` ×2）。

⚠️ **登记必须在**那一步的日志**之前**：喂数据的是 `add_game_log` 末尾的 `replay.note`，
迟一步这一帧就只记到"船没了"。

⚠️ **★ 本批实测踩到的第二个坑（值得记下来）**：`_append` 只在**下一步**追加时比对快照，
所以"撤销登记"只改状态、不推动记录器 ⇒ **这一帧永远不变**。实测：`handle_confirm_reinforcement`
的 `revive` 分支注册撤销之后，时间线里那一格照样是 `{'alive': False, 'sunk': True}`，
因为紧跟的 `_finish_placement` **不写任何日志**。修法：那一整段（复活 / 神机妙算 / 绝处逢生的
唯一一艘）收尾时显式 `replay.refresh_ships(room)`；且必须在**船的位置/船数/待办全部落定之后**
调（早一步记下来的是"船还不在 `ships` 里"的中间态）。`refresh_ships` 是幂等的
（`_record_snapshot` 自己比对，没变就不写）。

## E3. 四条腿（同一格"不再有船"的四种成因，只有牺牲该画成沉没）

| 腿 | 成因 | 真实入口 | 回放里必须 | 本批怎么钉 |
| --- | --- | --- | --- | --- |
| ★ 牺牲 | 恶魔契约 / 神之宣告 / 命运骰子 6 点 / 守株待兔 | `_do_demon_contract_sacrifice`（四种 reason） | **在时间线里且 `sunk`** | `tests/test_replay_ship_loss.py` 4 条参数化 + dom 工具 F 组 |
| ★★ 滥竽充数 | 临时船大回合末被强制收回 | `_recall_lanyu_ships`（`ships.remove`，**不登记**） | **不在时间线里** | 同名用例 + dom 工具反向腿② |
| ★ 换位 | 神机妙算重新部署 | `handle_confirm_reinforcement` 的 `shenji_redeploy` 分支 | 旧格**不留**红叉、新格是活船 | 同名用例 + dom 工具反向腿③ |
| ★ 复活 | 疗愈（原地）/ 死者苏生 | `_revive_sunken_ships` / `handle_confirm_reinforcement` 的 `revive` 分支 | 红叉**消失**、船重新画出来 | 两条用例 + dom 工具反向腿④ |

**怎么确认"真船移动"与"临时船收回"没被误判成沉没**：这两条腿都**走真入口**（前者真调
`handle_confirm_reinforcement`，后者在测试里走与 `_recall_lanyu_ships` 相同的 `ships.remove` 路径），
断言的是**逐格状态**（旧格必须**不在**时间线里 / 不许 `sunk`）——不是"数量相等"那种抓不住问题的判据。

## E4. 守卫怎么证明能红（每条都实测过）

| 守卫 | 改坏法 | 结果 |
| --- | --- | --- |
| ★★ 牺牲格必须留在时间线里（12 条） | 删掉 `note_ship_lost` 那一句（= 改前的行为） | **红 10 条**：`AssertionError: 牺牲的船格 (2,2) 不在回放的船位时间线里 ⇒ 回放棋盘上会凭空消失：[{'x': 3, 'y': 3, 'alive': True}]` |
| ★★ 滥竽充数反向腿 | **朴素修法**：`_snapshot` 把上一次快照里的船格并进这一次（曾有船就永远留成沉没） | **红**：`AssertionError: 临时船收回**不是沉没**…却在那格留下了沉没标记 ⇒ 回放里出现幻影沉船：{'x': 1, 'y': 1, 'alive': False, 'sunk': True}`（同一次改坏连红 4 条：换位 / 棋盘重摆 / 复活也一起红） |
| ★ 复活与换位反向腿 | 把 3 处 `note_ship_returned` 与收尾的 `refresh_ships` 一起去掉 | **红 3 条**（换位 / 复活 / 疗愈） |
| ★★ dom 工具 F 组（16 项） | ① 把 `replayFoldTimeline` 改成"只加不减"（= 朴素修法的前端等价物） | **红 7/62**，其中三条正是反向腿②③④（并顺带打脸这条判据的假绿可能） |
| ★★ dom 工具 F 组 | ② 后端只给 `alive:false` 却不给 `sunk`（口径不一致） | **红 7/62**，其中"牺牲格画成 ✕ / 带 `sunk` / title 写击沉"三条同时红 |

**"改前必须红"的原始输出**（第 1 条，改回缺陷写法后）：
`FAILED tests/test_replay_ship_loss.py::test_sacrificed_ship_cells_stay_in_the_timeline_as_sunk[demon_contract]`
（另有 `[divine_decree]` / `[dice_sacrifice]` / `[trap_sacrifice]` 与 6 条同族，共 **10 failed / 2 passed**）。

⚠️ **诚实说明这一批"红证"的分工**：`dom_replay_frame_check.mjs` 是**纯前端**工具
（它把手工 payload 直接喂给真 `replayComputeFrame` + 真 `renderReplayBoard`），
所以它的 F 组**证明的是"前端能把牺牲格画成红叉"与"另外三种成因不会长出沉没"**，
**不是**"后端会产出那样的 payload" —— 后者由 pytest 那条真调实现点的守卫负责。
两个改坏变体下**只有 F 组的正面腿仍绿**（因为喂进去的 payload 是好的），
这正是上一条那个分工的必然结果，不是判据失灵。

## E5. 工具与真浏览器 E2E

| 工具 | 次数 | 结果 |
| --- | --- | --- |
| `node --check static/game.js` / `tools/*.mjs` | 每次改动后 | 全绿 |
| `node tools/dom_replay_frame_check.mjs` | **3 次（加 F 组之后；没加 F 组之前还跑过 3 次，每次都是 46 项全绿）** | **3/3 绿（62 项，46 → 62）**；另跑 2 个内存改坏变体，**2/2 都红**（见 E4） |
| `node tools/dom_contract_check.mjs` | 1 次 | **1/1 绿** |
| `node tools/replay_check.mjs`（真浏览器 E2E，5097 端口 + 独立库） | **3 次** | 第 1 次 1 项红（**工具脆性**，见下）；第 2 次 6 项红（**工具挑错局**，见下）；修掉之后 **连跑 2 次 2/2 全绿** |
| `node tools/spectate_check.mjs`（四浏览器 + 不变量 1 + 自我校准腿） | 2 次 | 第 1 次红在**前提**（`/register` 撞上「同 IP 60 秒 10 次」限流，前面几个工具把配额用光了，与代码无关）；等 70 秒后 **1/1 全绿**（`badCount:0`、自我校准腿 `badCount:4`） |
| `python -m pytest tests/ -q` | 3 次 | **2431 passed**（基线 2419，**只多不少**） |

⚠️ **`replay_check.mjs` 第 2 次那 6 项红是工具自己的**（不是产品缺陷），根因与修法都写进注释了：
挑行判据原来是 `attacks > 0`，会挑到"被放弃的小局"（AI 先手一炮清场，**4 步 / 1 炮**），
于是下面几条**固定步数**的断言假红（`连点 6 次下一步后 k=6` 读到 `[1,2,2,2,2,2]`、节点数也不等）。
改成 `attacks >= 6`（本工具打的就是"6 炮收工"的局）之后连跑 2 次全绿。
第 1 次那 1 项红是倍速（4× 首步 939ms vs 1× 938ms），**重跑即消失**（第 2/3 次分别是 253/250ms）——
是已记录的 flakiness，本批**没有改动倍速那一段代码**。

**浏览器实际执行的那一份 `game.js`**（D6 那条防缓存探针的原始输出，本批没改 `game.js`，探针反映的就是它）：
```
浏览器执行的那一份 game.js: {"hasAfter":false,"hasLastReset":true,
  "src":"http://127.0.0.1:5097/static/game.js?v=1790156136","bytes":771245,"dur":90}
```
（`hasAfter:false` 是**对的**：`var after = false;` 是 D2 修掉的那段缺陷写法，现在源码里已经没有了。）

**牺牲端到端腿**：`replay_check.mjs` 新增了一段"牺牲腿"，但**本批 3 次跑的人机局都没有发生过
主动牺牲**（手牌随机），工具**如实打印**
`牺牲腿: 本局**没有**发生主动牺牲（手牌随机）⇒ 这条端到端腿本轮未覆盖` 并跳过 ——
**没有**为了凑一条而写恒真的假断言。这条判据由 `tests/test_replay_ship_loss.py`（真调
`_do_demon_contract_sacrifice`）与 dom 工具的 F 组（逐格断字形）覆盖。

## E6. 体积（改后实测）

真对局 10 局（`tools/headless_game.py`，seed 4242~4251，master vs random）：

| 项 | 实测 |
| --- | --- |
| blob | 中位 **31676 B（30.9 KB）** / 最坏 **289239 B（282.5 KB）**（后两局是 1403 / 1463 步的长局） |
| 同一批局**改前 vs 改后**逐局比对 | **10/10 差值 +0 B**（这批自对弈局一次主动牺牲都没发生 ⇒ 这条修复在真实局里的成本是 **0 B**） |
| 【上界】153 步那一局灌入 16 次牺牲（两侧交替、每次一格） | 38799 B → 43284 B，**+4485 B（+4.4 KB）**，**平均每次牺牲 280 B** |
| 硬上限 | 512 KB（还差得很远；一次牺牲 280 B 里大头是"重发该侧整份船位"的 229 B/步级开销，不是那 52 B 的格子） |

⚠️ 与上一批 D8 的 26.9 KB 中位**不能直接相减**（两批跑的是**不同的局**，本批含 1403/1463 步的长局）。
能对比的是"同一批局改前改后 +0 B"这一行 —— 那才是本批的增量。

## E7. 公告

`changelog.py` 最上面加一条，`date = 2026-09-23 23:55`（真实时间，与上面那条 17:27 严格降序）：

- `修复了：回放里主动牺牲的战舰直接消失，现在会留下沉没的叉叉`

## E8. 本批一并接上的**另外两个自牺牲实现点**（照任务要求"发现第二条路径就停下来说"）

任务给的定位是"所有 reason 都汇到 `_do_demon_contract_sacrifice`"——**这句话对四处
reason 是准确的**，但 grep `ships.remove(` 之后发现**同一件事还有两个实现点**，
它们同样"船从 `player.ships` 消失 + 记进 `sunken_ships` + 广播 `ship_sacrificed`"，
却**不走**那个函数（作者报的现象在它们身上**一模一样**）：

| 实现点 | 触发 | 广播的 reason | 本批怎么处理 |
| --- | --- | --- | --- |
| `apply_magic_effect` 的 **`绝处逢生`** 分支 | 手动打出（牺牲**全部**战舰） | `last_stand` | **接上了** `note_ship_lost`（逐艘，与它逐艘广播同一份数据） |
| `apply_magic_effect` 的 **`钢筋铁骨`** 分支 | 手动打出（牺牲一艘换无敌） | 无（只有日志） | **接上了** `note_ship_lost` |

⚠️ 这两处**没有**走"统一到 `_do_demon_contract_sacrifice`"的重构（那会改变 `trap` /
无敌 / `_on_ship_destroyed` 结算顺序等既有语义，风险远大于收益），
而是用**同一个登记入口**（`note_ship_lost`）把它们接进同一条时间线 ——
判据仍然只有一份（"哪些格是主动牺牲"，由 `_ship_cells` 读同一张表）。

**还有第五条同族成因，本批没改**：**`神威！` 把船从 `ships` 里删掉**
（`del target_player.ships[i]`）。其中：

* **暂时除外**那一支（`game_effects['excluded_ships']`）是**有意不登记沉没**的 ——
  棋盘上那片区域会画成"被扣掉的洞"（本批 `effects` 时间线已经能画出来），**该保持现状**；
* 但**致死**那一支（`board != 'self' and len(excluded_ships) == 1` ⇒ `_mark_ship_sunken`）
  在回放里那一格同样是**凭空消失**（它不产生 `attack` 步、也没有"沉没"记录）。
  这是同一个缺陷的**第三个**实现点，本批**没有动它**：作者只报了"主动牺牲"，
  而这一支要单独想清楚"洞 + 沉没"两个视觉效果怎么共存。**留给下一批**。

## E9. 仍未验证（诚实清单）

1. **牺牲的浏览器端到端**：`tools/replay_check.mjs` 打的人机局**一次牺牲都没发生**
   （手牌随机抽的），所以"真浏览器里点开一局含牺牲的回放，看到红叉"这件事**没验证过** ——
   由 pytest（真调实现点）与 dom 工具（喂手工 payload 给真 `replayComputeFrame` + 真渲染）覆盖。
2. **`dice_sacrifice` / `trap_sacrifice` / `divine_decree` 三条 reason 的真实触发链路**
   （命运骰子摇到 6 点 / 守株待兔陷阱被打沉 / 神之宣告出牌）**没有**在真对局里跑过 ——
   本批是**直接调 `_do_demon_contract_sacrifice`**（唯一实现点）验的，四条 reason 只是参数化换了名字。
3. **`绝处逢生` / `钢筋铁骨` 的自牺牲**（本批新增登记的两个点）**没有**跑真对局，
   只有源码级守卫钉住"调用点白名单"。它们**没有**反向腿（绝处逢生之后棋盘会重摆，
   钢筋铁骨之后船不回来）。
4. **一条真回放里同时出现"牺牲 + 棋盘重摆"**（`note_board_replaced` 撤销登记）没跑过真对局，
   只有单测（`test_board_replace_drops_the_previous_wrecks`）。
5. **线上实测**：要等部署之后才能验（生产库里的老回放都是本批之前落的，不会有牺牲格）。
6. **多艘船在一次结算里同时牺牲**（神之宣告两艘、命运骰子两艘）的后端形状
   只有"逐艘调用"的单测覆盖，**没有**端到端验过。
7. **2000 步极端局**的体积仍只有上界估算（契约 §11.3 本来就标了不覆盖）。

---

# 实施记录：`神威！` 致死那一支的船在回放里凭空消失（2026-09-24 凌晨）

> 本节同样是**追加**的实施记录。§0~§11（契约）与 R1~R11、D1~D10、E1~E9（前三批的实施记录）
> **都没有改动一个字**。本批修的是"某格不再有船"五个成因里的**最后一个**（第 5 类），
> 外加一件"防下一次"的事：把这类位置**穷举**成一条源码级守卫。

## F1. 根因（后端，`server.apply_magic_effect` 的 `神威！` 分支）

```python
excluded_ships.append(ship)
del target_player.ships[i]            # ★ 船从列表里被摘掉
target_player.remaining_ships -= 1
...
if board != 'self' and len(excluded_ships) == 1:      # ← 致死那一支
    _mark_ship_sunken(target_player, excluded_ships[0])
```

`replay._ship_cells` 只遍历 `player.ships` ⇒ 那一格不再进快照 ⇒ 前端的增量叠加
（按 `side` 整体替换）把它换掉了 ⇒ 回放棋盘上那格**凭空消失**。
与 E1 的牺牲缺陷**同一个形状**，只差一个实现点。

**为什么前四批都没碰到它**：`神威！` 致死**不产生 attack 步**（没有"打这一炮"），
所以连攻击标记也补不上 —— 而同样 `ships.remove` 的 `轰炸` / `硫磺火焰` 逐格发
`attack_result` 并写 `caster.attacks`，回放靠攻击标记本来就画得出红叉。
**判据因此只有一句话："这一处移除会不会产出攻击步"** —— 不会的就必须显式登记。

**改前 / 改后（真实数据，`.tmp/dbg_shenwei.py` 真调 `apply_magic_effect` 读出来的时间线）**：

```
改前: ships=[{step:0, p1:[(0,4)活,(1,4)活], p2:[(1,1)活,(5,5)活]},
             {step:1, p2:[(5,5)活]}]                 ← (1,1) 整格消失
改后: ships=[{step:0, p1:[(0,4)活,(1,4)活], p2:[(5,5)活,(1,1)沉没]}]   ← 沉没格留下
      effects=[{step:0, p2:{shenwei_holes:[{0,0,2,2}], ...}}]
      DOM: (1,1) -> {"text":"✕","cls":"cell ship sunk hit",
                     "aria":"神威：这一格被扣掉，暂时打不到（击沉：这一格的战舰已被打沉
                             （神威把这一格从棋盘上扣掉了，这一格的战舰不会再回来））"}
改前同格 DOM: {"text":"","cls":"cell replay-water shenwei-hole"}   ← 只剩一个洞，船没了
```

## F2. 改法：**同一个登记入口**，而且是**有意与"暂时除外"分开**

| 支 | 触发 | 游戏里 | 回放里 | 本批怎么改 |
| --- | --- | --- | --- | --- |
| **致死** | 作用于对方棋盘 + 区域内恰好 1 艘 | 船被就地打沉 | **红叉** | 在那一支里加 `replay.note_ship_lost(room, target_id, 这艘船的坐标)` |
| **暂时除外** | 其余情况（含打自己棋盘） | 区域被扣掉，船下回合原样归还 | 那一格**消失** | **一个字都不动**（`shenwei_holes` 区域高亮保持原样） |

数据源就是 `excluded_ships[0].positions`（**调用方手上那艘真船**），
**没有**任何"diff 前后快照猜哪几格没了"的写法 —— 那会把暂时除外的船一起判成沉没
（E2 已经点名禁过）。

**前端只改了两处（都在 `game.js` 的回放屏里，观战/实战一个字都没碰）**：

1. `replayCellOf`：沉没格**又在神威洞里**时，在无障碍文案后面补一句
   `（神威把这一格从棋盘上扣掉了，这一格的战舰不会再回来）`。
   作者口径是"格子按沉没画，'被挖掉'这条信息放 title/无障碍文案里"；
   ⚠️ 文案必须点名"**不会再回来**"：效果那一层的 label 已经把"这一格被扣掉，暂时打不到"
   说过了，只重复那句话的话，它对这个沉没格与"区里其它洞"是**同一句**（判据分不开）。
2. `renderReplayBoard`：**沉没格不叠效果类**。`.cell.shenwei-hole` 的
   `background: repeating-linear-gradient(...) !important` 会**盖掉**红色沉没底
   （`.cell.hit` 那份只是普通 background）⇒ 不改的话画出来是一片斜纹、不是红叉。
   这条只在**沉没**时短路，护盾 / 冻结 / 绝处逢生的候选格照旧叠加。

## F3. 记录器侧：**同一个 step 只留一行**（本批实测抓到的第二个坑）

`_record_snapshot` 是"同一帧可以记很多次"的（`_append` 每追加一步就记一次），
而"船真的变了"可能在同一节里发生**第二次**（复活类收尾、本批的"致死登记 + 神威洞"）。
实测：不处理的话时间线里会出现 `[{step:0,…},{step:0,…}]` ——
增量语义下这是**同一帧的两份说法**（前端逐条叠加虽然"后者盖前者"看着对，
任何按 `step` 去重的消费者都会算错，而且白占体积）。

改法：`_record_at_current_step` 记完之后调 `_merge_same_step_ships_rows(st, step)`
—— 判据只看"最后两行是不是同一步"，所以对本来就没有重复的时间线是**空操作**。

⚠️ **顺带量到的账**：这一层在同批 10 局里**净省了 1256 B**（三局各 −234 / −861 / −161，
其余 7 局 ±0）—— 那三局是"同一步里船位被记了两次"的局，与 `神威！` 无关。
**本次修复本身在真对局里的字节成本是 0**（下面 F6 有实测）。

## F4. ★ 穷举：全项目"船从 `player.ships` 消失 / 被替换"的**全部**位置

做了两轮 grep（先正则全量、再用 AST 复核），**共 27 处点**碰 `*.ships`（`_ai_place_ships`
那一处"整体重建 + 逐艘 append"算一个点），其中 **12 处会"移除/替换"**、
**15 处只增不减或构造器/夹具**。逐处表态：

| # | 位置（函数 + 容器 + 操作 + 卡片分支） | 属于哪一类 | 该不该登记沉没 | 本批 |
| --- | --- | --- | --- | --- |
| 1 | `_do_demon_contract_sacrifice` `player.ships.remove` | ① 主动牺牲 | **要**（4 种 reason 全汇这里） | 上一批已接 |
| 2 | `apply_magic_effect` / `绝处逢生` `caster.ships.remove` | ① 自牺牲 | **要** | 上一批已接（E8） |
| 3 | `apply_magic_effect` / `钢筋铁骨` `caster.ships.remove` | ① 自牺牲 | **要** | 上一批已接（E8） |
| 4 | **`apply_magic_effect` / `神威！` `del target_player.ships[i]`** | **⑤ 致死（本批）** | **要**（致死那一支）/ 除外那一支**不许** | ★ **本批修** |
| 5 | `apply_magic_effect` / `轰炸` `opponent.ships.remove` | ② 有攻击步 | 不必（逐格 `attack_result` + `caster.attacks`） | 不动 |
| 6 | `apply_magic_effect` / `硫磺火焰` `opponent.ships.remove` | ② 有攻击步 | 同上 | 不动 |
| 7 | `_recall_lanyu_ships` `player.ships.remove` | ③ 有意消失 | **绝不许**（卡面"不会显示沉没"） | 不动 |
| 8 | `confirm_magic_target` `player.ships = []`（灵气复苏） | ④ 棋盘换新 | 由 `note_board_replaced` 撤旧账 | 不动 |
| 9 | `apply_magic_effect` / `败者食尘` `player.ships = []` | ④ 棋盘换新 | 同上 | 不动 |
| 10 | `apply_magic_effect` / `回光返照` `caster.ships = []` | ④ 棋盘换新（只自己那块） | 同上 | 不动 |
| 11 | `handle_place_ships` `room.players[pid].ships = [...]` | ④ 布船阶段重建 | 不必（还没开打） | 不动 |
| 12 | `_ai_place_ships` `player.ships = []` + `append` | ④ 布船阶段重建 | 同上 | 不动 |

只增不减 / 构造器 / 夹具的 15 处（`Player.__init__`；4 处调试事件夹具
`test_set_player_ships` / `test_reset_game` / `test_set_opponent_ships`；
`_revive_sunken_ships`；`handle_confirm_reinforcement` ×5；
`_restore_due_shenwei`；`平等条约`回滚；`_ai_place_ships` 的 append）
不可能造成"消失"，一并登记在表里备查。

**★ 有没有第 6 处（前一批也没发现的）？** —— 有**一个半**，本批都**没修**、如实记下来：

* **半处：`轰炸` / `硫磺火焰` 里"已经沉过的船"那一格**。这两处逐格发
  `attack_result` 的那份攻击表是 `caster.attacks`，而**回放重建棋盘用的正是攻击步**
  ⇒ 一般情况下画得出红叉。但它们**同时**把船从 `ships` 里摘掉，而
  `_mark_ship_sunken` 还会把 `hits` 打满 —— 于是**"这一格到底是沉没还是空海"在两套
  数据源里各有一份说法**。本批**没有**构造出真实症状（要打一局"轰炸把已沉船所在格
  再盖一次"的局），**没有验证**，所以按"不修但写明"处理（见 F8 第 3 条）。
* **真第 6 处（不是移除，是"同一件事的第二份实现"）**：`replay._ship_cells` 的
  "沉没"判据（`alive: False` + `sunk: True`）现在有**两个**来源
  —— ① 活船列表里 `len(hits) < len(positions)` 为假；② `lost` 表（显式登记）。
  这不是缺陷（②就是为"船已经不在 `ships` 里"准备的），但**两块表的合并顺序**决定了
  同一格出现两次时前端取哪一条 —— 目前 `_lost_cells` 追加在**后面**，
  前端 `replayCellOf` 取**第一条**命中的 ⇒ 若同一格既在活船里又在 `lost` 表里，
  画的是"活船"。**这条没有用例守着**（本批也没构造出那个形状），列进 F8。

## F5. 守卫清单（每条都实测过"能红"）

| 守卫 | 改坏法 | 原始结果 |
| --- | --- | --- |
| ★★ `tests/test_replay_shenwei_kill.py`（12 条） | 删掉致死那一支的 `note_ship_lost`（= 改前） | **红 5 条**（`test_shenwei_kill_marks_the_cell_as_sunk` 等）+ `test_ship_removal_sites.py` **红 2 条** |
| ★★ 同文件 | 登记在、但删掉收尾的 `refresh_ships(room)` | **红 4 条** |
| ★★ 反向腿（暂时除外） | **朴素修法**：把登记挪到 `del target_player.ships[i]` 旁边 | **红 6 条**，其中正是"暂时除外/自扣棋盘/kill 与 exclude 必须不同"三条反向腿 |
| ★★ `tools/dom_replay_frame_check.mjs` G 组（新增 17 项 → 全表 78 项） | 去掉 `!item.cell.sunk` 判断（沉没格照样叠斜纹） | **红 1/78**（`致死格不再叠洞的斜纹`） |
| ★★ 同上 | 去掉"被挖掉 / 不会再回来"那句文案 | **红 1/78** |
| ★★ `tests/test_ship_removal_sites.py` 穷举左腿 | 新加一个 `room.players[...].ships.remove(ship)` | **红 1 条**：`这些"改 ships 的点"没有在 REMOVAL_SITES 里交代 … _recall_lanyu_ships:…` |
| ★★ 穷举右腿 | 从登记表里删掉 `神威！` 那一条 | **红 1 条**（两条腿都报：`没有交代` + `note_ship_lost 调用点与登记表对不上`） |
| ★ `tests/test_replay_shenwei_kill.py` | 去掉"同一步只留一行"的合并 | **红 7 条** |
| 对照（不该红） | 什么都不改 | **31 passed**（神威 12 + 穷举 8 + 牺牲批 11） |

**前四类反向腿一起复跑**（同一批，全部保持绿）：
`test_replay_ship_loss.py` 11 条 —— 牺牲 4 种 reason 仍是沉没 /
滥竽充数收回仍消失 / 换位与复活仍不留假红叉；dom 工具 F 组的反向腿②③④同样全绿。

## F6. 体积实测（本批：修 + 合并两层）

真对局 10 局（`tools/headless_game.py`，seed 4242~4251，master vs random）：

| 项 | 实测 |
| --- | --- |
| blob | 中位 **31596 B（30.9 KB）** / 最坏 **288378 B（281.6 KB）**（1403 / 1463 步那两局长局） |
| 同一批局**改前 vs 改后**逐局比对 | **合计 −1256 B**；逐局差值集合 `[-861, -234, -161, 0]`（省的全部来自 F3 的"同一步合并"，与神威无关） |
| 这 10 局里 `神威！` 的施放次数 | 4 次（4242 / 4244 / 4246 / 4250 各 1 次），**4 局全部 ±0 B** |
| 【上界】153 步那一局（seed 4248）灌入 N 次"致死登记" | 1 次 +260 B；4 次 +380 B；16 次 +860 B（**每多一次约 54 B**，一格沉没的稳态成本） |
| 硬上限 | 512 KB（还差得远） |

⚠️ 与上一批 E6 的 30.9 KB 中位**恰好相同**（改前中位是 31676 B ≈ 30.9 KB，也是 30.9 KB），
能对比的是"**同一批局改前改后 −1256 B**"这一行 —— 那才是本批的净增量。

## F7. 公告

`changelog.py` 最上面加一条，`date = 2026-09-24 00:56`（真实时间，与上面那条 23:55 严格降序）：

- `修复了：回放里被神威打沉的战舰凭空消失，现在会留下沉没的叉叉`

## F8. 仍未验证（诚实清单）

1. **浏览器端到端**：本批跑的两次 `tools/replay_check.mjs`（真打完一局）里，
   那一局的 AI 只打出【雷达子弹】【教皇旨意】【克苏鲁之眼】，**没有打出 `神威！`**
   —— 所以"真浏览器里点开一局含神威致死的回放、看到红叉"这件事**没有验证**。
   工具**如实打印**了这一点（"牺牲腿本轮未覆盖"那条同款机制），
   本批**没有**为它写恒真的假断言。覆盖它的是 pytest（真调 `apply_magic_effect`）
   与 dom 工具 G 组（喂手工 payload 给真 `replayComputeFrame` + 真渲染）。
2. **后端产出的 payload 形状**由 pytest 断言；dom 工具的 G 组证明的是"前端能把那样的
   payload 画成红叉" —— 两者分工与本项目前几批一致（不是判据失灵）。
3. **`轰炸` / `硫磺火焰` 把"已沉船所在格"再盖一次**那种局没构造出来，
   "回放里那一格该是沉没还是空海"**没有验证**（F4 的"半处"）。
4. **同一格同时出现在活船列表与 `lost` 表**时的取哪一条**没有用例守着**（F4 的真第 6 处）。
5. **`神威！` 致死 + 棋盘重摆（回光返照/灵气复苏/败者食尘）在同一步里**没跑过真对局，
   只有 `note_board_replaced` 的既有单测；"重摆把致死格的登记撤掉"这条路**没有**针对神威验过。
6. **`_merge_same_step_ships_rows` 对"三步以上同 step"**（理论上不会出现）没有用例。
7. **线上实测**：要等部署之后才能验（生产库里的老回放都是本批之前落的，不会有神威致死格）。
8. **`replay_check.mjs` 的缓存探针**本轮报的 `bytes` 是 757778，而磁盘上 `static/game.js`
   是 757478 B（差 300 B，`transferSize` 是传输量、不是文件长度）——
   "浏览器执行的就是改后那一份"是**按探针的版本参数 + 源码标记**判定的
   （磁盘源码里 `这一格的战舰不会再回来` 与 `if (!item.cell.sunk) {` 都在），
   **不是**逐字节比对。

---

# 实施记录：沉没判据收口（两份数据源 → 一份 + 一条清理路径）（2026-09-24 凌晨）

> 本节同样是**追加**的实施记录。§0~§11（契约）与 R1~R11、D1~D10、E1~E9、F1~F8
> **都没有改动一个字**。本批收的是 §F8 自己如实报告的那**两处没修的**（第 3、4 条），
> 两处都是同一个形状 —— **同一件事有两份数据源**（教训 #1）。

## G1. 口子①：沉没判据双源 —— **口径定为「显式事实优先」**

| 数据源 | 是什么 | 性质 |
| --- | --- | --- |
| 活船列表（`player.ships` 快照） | "这艘船**不在**列表里 ⇒ 它沉了" | **否定式推断** |
| `lost` 登记表（`replay.note_ship_lost`） | "这艘船因恶魔契约被献祭了" | **实现点当场登记的事实** |

**取 `lost`**。理由与反向理由都写进了 `replay._ship_cells` 的 ★★ 段（代码注释就在判据旁边）：

* 事实压过推断：`lost` 是实现点当场写下的"这艘船怎么了"，活船列表那份只是"它不在列表里"；
* **反过来会让"看不见"盖住"看得见"**：`lost` 列的格子在游戏里是**双方都看得见的沉没**
  （牺牲时那条广播原文：「公开广播：自己与对手都能看到这艘船被划掉」），
  画成活船 = 在一格已经公开划掉的格子上重新画一艘船（幻影活船）。

**实现方式：不是"两条里挑一条"，而是排除这个状态本身。** `_ship_cells` 保证
**同一格最多只出一条**（撞车时活船那条整条丢掉），于是前端 `replayCellOf` 的
`for…break`（取第一条）与"以最后一条为准"**结果相同** —— 口径**不依赖前端取哪一条**。
每条船格多带一个探针字段 `src`（`'ships'` / `'lost'`），它同时是"一格一条"的证据。

## G2. 口子①的第三问：`lost` 表的清理路径**逐条表态**

| # | 路径（实现在哪） | 撤销口 | 本批 |
| --- | --- | --- | --- |
| 1 | 复活（`_revive_sunken_ships`） | `note_ship_returned` | ✅ 已有（上一批） |
| 2 | 换位（神机妙算重新部署：`handle_confirm_reinforcement` 的 `shenji_redeploy` / `revive` 两支） | `note_ship_returned` | ✅ 已有（上一批） |
| 3 | 棋盘整块换新（灵气复苏 / 败者食尘 / 回光返照 / 绝处逢生） | `note_board_replaced` | ✅ 已有（上一批） |
| 4 | **★ 平等条约回滚**（`apply_magic_effect` / `平等条约` 分支） | **没有** | ★ **本批补** |
| 5 | 神威"暂时除外"到期归还（`_restore_due_shenwei`） | 不需要 | 那一支**有意不登记** `lost`（F2 定的口径） |

**第 4 条是真的会漏**（不是理论推演），机制是 `last_ship_change` 是**单槽**、而"牺牲"
不写这个槽：

```
A 船被魔法击沉（写 last_ship_change，ship=A）
  → B 船被牺牲（写 lost[B]=B 的格；**不动** last_ship_change）
  → 对手打【平等条约】：快照里还是 ship=A ⇒ 回滚把 **A** 放回 ships
     ⇒ A 的 lost 登记**没有任何人撤** ⇒ 那一格在回放里是个红叉，可它在游戏里活着
```

实测症状（真调两条产品路径，`.tmp/dbg_lost.py` 场景 2 与 `.tmp/dbg_lost2.py`）：

```
回滚前: p2 = [{'x':4,'y':4,'alive':True}, {'x':1,'y':1,'alive':False,'sunk':True}]   ← 红叉
平等条约 → success=True message=成功无效化船数改变效果
回滚后: victim in p2.ships = True   hits=[]   lost 表 = {'lp2': [(1, 1)]}            ← 船活着
        时间线里那一格仍然是 {'alive': False, 'sunk': True}                          ← **幻影沉船**
修后  : lost 表 = {}   时间线那一格 = {'x':1,'y':1,'alive':True,'src':'ships'}
```

改法：回滚分支里（船真的被放回 `ships` 之后）调 `replay.note_ship_returned`，
并在该分支收尾调 `replay.refresh_ships(room)` —— 这个分支**不写游戏日志**，
不补那一次快照，红叉会一直挂到下一次有日志的动作为止（同 F5 的坑）。

## G3. 口子②：**构造出来了，画得是<u>对</u>的**（不改逻辑，只钉现状）

上一批（§F4"半处" / §F8 第 3 条）只写了推断、**没验证**。本批按"先复现再改"做了三种真实局面
（真调 `handle_attack` + 真调 `apply_magic_effect`，`.tmp/dbg_bomb.py`）：

| 局面 | 那一格的最终状态 | 回放帧里画成 |
| --- | --- | --- |
| A 两格竖船 (1,1)-(1,2)，炮击 (1,1) **没沉**，再轰炸第 1 行 | `{'x':1,'y':1,'alive':True,'src':'ships'}`；mark `{hit:True,sunk:False}` | `✕`（命中·有船） |
| B 单格船 (1,1) 被炮击**击沉**，再轰炸第 1 行 | `{'x':1,'y':1,'alive':False,'sunk':True,'src':'ships'}`；mark `{hit:True,sunk:True}` | **`✕` 沉没**（= 沉没，不是空海） |
| C 同 B，但用【硫磺火焰】覆盖那一列 6 格 | 同上 | **`✕` 沉没** |

**为什么对（哪一份数据源兜住的）**：`轰炸` / `硫磺火焰` 的候选表有一条**存活过滤** ——

```python
to_remove = [ship for ship in opponent.ships
             if len(ship.hits) < len(ship.positions) and any(...)]
```

`_mark_ship_sunken` **不会**把 `hits` 打满（本项目的击沉不移出 `ships`），
所以"已经沉过的船"`len(hits) == len(positions)` ⇒ **压根不进候选表**：
它既不会被二次摘掉，也不会再补一条 `ship_sunk=False` 的落空标记。
那一格的沉没因此**由活船列表那一份独立兜住**（`src` 也是 `'ships'`，不是 `lost`）——
**两套数据源在这一格上给出的是同一个答案**，上一批担心的"各有一份说法"没有发生。

⇒ 按任务要求：**不改这两条逻辑**（正确的东西不许为了"看起来一致"去动），
改为给现状加**防变坏守卫**（G4 第 3 组 + dom 工具 H 组）。

**⚠️ 口径澄清（本批顺带更正）**：§F4 写的"`_mark_ship_sunken` 还会把 `hits` 打满"
**不成立** —— 那个函数只往 `sunken_ships` 里 append（带去重），不碰 `hits`。
真正把 `hits` 打满的是 `绝处逢生` 的自牺牲分支（`sh.hits = list(sh.positions)`）。
那几句话是上一批的**推断没验证**留下的，本批按实测更正（契约段一个字没动，只在本节说明）。

## G4. 守卫清单（本批新增的每一条都实测过"能红"）

| # | 守卫 | 改坏法（红证） | 原始结果 |
| --- | --- | --- | --- |
| 1 | `test_replay_lost_priority.py::test_lost_fact_wins_over_alive_ship_inference` | 去掉 `_ship_cells` 的撞车过滤 | **红**：`同一格必须只有一条说法…assert 2 == 1` |
| 2 | 同上（探针字段） | 把 `lost` 那批的 `src` 标成 `'ships'` | **红**：`这一格必须按显式事实（lost 表）出：…'src': 'ships'` |
| 3 | `…::test_treaty_rollback_clears_the_wreck_it_revives` 等 3 条 | 删掉平等条约分支的 `note_ship_returned` | **红 4 条**（含"回滚之后 `lost` 必须清空"） |
| 4 | `…::test_treaty_rollback_snapshot_lands_on_the_same_step` 等 2 条 | 删掉收尾的 `refresh_ships` | **红 2 条**（`船已经回到棋盘上，那一格的红叉必须消失`） |
| 5 | `…::test_lost_table_is_empty_after_every_cleanup_path` 等 2 条 | 删掉疗愈的 `note_ship_returned` | **红 2 条** |
| 6 | `…::test_every_ship_add_site_states_its_lost_policy`（左腿） | 凭空加一个 `player.ships.append(ship)` | **红 1 条**：`_redproof_new_add_site / player 第 5168 行` |
| 7 | 同上（右腿） | 把 `note_ship_returned` 搬到别的函数里 | **红 2 条**：`登记"要撤销"，但这个函数里没有 note_ship_returned` |
| 8 | `tools/dom_replay_frame_check.mjs` H 组 | 让 `replayCellOf` 按 `src` 挑格子 | **红 2/85** |
| 9 | 同上 | 去掉 `; break;`（改成"以最后一条为准"） | **红 2/85** |
| 对照（不该红） | 什么都不改 | `pytest tests/test_replay_lost_priority.py` **8 passed**；dom 工具 **85/85** |

**反向腿一起复跑（四类，全部保持绿）** —— 同一批：
`test_replay_ship_loss.py` **9 passed**（牺牲 4 种 reason 仍是沉没 / 滥竽充数收回仍**消失** /
换位与复活仍不留假红叉）、`test_replay_shenwei_kill.py` **12 passed**（"暂时除外"仍不登记沉没）、
`test_ship_removal_sites.py` **8 passed**（穷举移除表）、dom 工具 F 组反向腿②③④与 G 组全绿。

## G5. 体积实测（本批：船格多一个 `src` 字段）

真对局 10 局（`tools/headless_game.py`，seed 4242~4251，master vs random），
**同一批局改前/改后逐局比对**（改前那份是**内存里**去掉 `src` 等价出来的，磁盘没动）：

| 项 | 实测 |
| --- | --- |
| 改后 blob | 中位 **33075 B（32.3 KB）** / 最坏 **289666 B（282.9 KB）** |
| 改前 blob（同批 seed） | 中位 **31596 B（30.9 KB）** / 最坏 **288378 B（281.6 KB）** |
| 逐局净增量 | `+1932 / +1120 / +1288 / +168 / +1456 / +1526 / +2016 / +910 / +1379 / +518` ⇒ **合计 +12313 B** |
| 折合 | 每局约 **+1.2 KB**（≈0.5~1.6%），最坏那局 **+1288 B = +0.45%** |
| 硬上限 | 512 KB（还差得远） |

⚠️ 改前的 31596 B 与上一批 F6 记的 **31596 B 完全一致** ⇒ 这一批**没有**顺手动到别的体积因素，
"+12313 B"这一行就是本批的净增量（`src` 是每个船格一次，约 17 B/格）。
判据：这条字段买到的是"**同一格只有一份说法**"可被断言 + 可被探针看见；
不想要它的话可以把 `_ship_cells` 里遍历 `sorted(lost_set)` 当成隐式判据（省 ~1.2 KB/局），
但那样"优先级"就只存在于代码顺序里、数据上看不见了 —— 本批按"可断言优先"保留。

## G6. 公告

**不加 changelog**。理由（本批偏内部正确性）：

* 本批改的是**回放的内部口径与清理路径**，且**没有可复现的玩家可见变化**
  （口子② 实测画得**是对的**，一个字都没改逻辑；口子① 的第三条路径需要
  "A 船被魔法击沉 → B 船被牺牲 → 对方手里有平等条约"三条同时成立，属罕见对局）；
* `docs/UPDATES_2026_09_19.md` 的规矩是**给玩家读的**，内部口径收口不该占一条。

## G7. 仍未验证（诚实清单）

1. **口子①的口径是"纸面 + 单测"钉住的，不是"线上观测到过错画"** ——
   第三条清理路径（平等条约回滚）**没有在真对局里撞到过**：本批是**直调产品函数**
   构造出来的（`_do_demon_contract_sacrifice` + `apply_magic_effect`），
   没有跑一局"魔法击沉 A → 牺牲 B → 平等条约"的**真 3 人链**（需要特定手牌与出牌顺序）。
   判据：`last_ship_change` 的单槽语义 + `lost` 表的登记点，两条都在源码里可读；
   **但"真对局里撞上的概率"没量过**。
2. **口子②只构造了三种局面**（A/B/C 见 G3）：两格船被炸掉一格、单格船先沉后炸/后烧。
   **没有**构造"爆炸同时命中多艘、其中一艘已沉"的组合局，也没有构造
   "已沉船 + 重摆棋盘之后再炸同一格"的局。
3. **`src` 字段的线上兼容**：老回放（本批之前落库的）**没有** `src` —— 前端不读它，
   所以显示不受影响；`tests/test_replay_recorder.py` 里那条字段白名单断言放宽成
   `<= {x,y,alive,sunk,src}` 就是为了这个（老 blob 少一个字段照样过）。
   **但"拿旧 blob 走一遍读接口 + 前端渲染"这件事本批没跑**（本机没有线上库的拷贝）。
4. **浏览器端到端只跑了 `replay_check.mjs` 两次**：第 2 次全绿，那一局的
   `牺牲腿` 与"神威致死格"**都没覆盖**（工具如实打印了）。本批新增的 H 组是
   **喂手工 payload** 给真 `replayComputeFrame` + 真渲染，不是真对局数据。
5. **`spectate_check.mjs`（观战四条不变量）**：本批改了 `server.py` 的 `平等条约` 分支，
   按规矩跑了 —— **1 次全绿（156 条 PASS）**，没有出现 `/register` IP 限流那种假红。
6. **`lost` 表的第 6 条路径**：本批只核了 `server.py` 里"会把船放回 `ships`"的点
   （`tests/test_replay_lost_priority.py` 的源码级穷举守卫覆盖 **20 个**追加点 /
   **13 条** `(函数, 容器)` 登记）；`ai_brain.py` / `spectate.py` 等其它模块
   没有类似"复活"的语义，但**没有**做过全项目 AST 复核
   （`test_ship_removal_sites.py` 的宽表扫的是移除侧）。
7. **`replay_check.mjs` 的缓存探针**本轮报 `bytes = 758480`，而服务端
   `Content-Length: 758180`（磁盘上 `static/game.js` 也是 758180）——
   差的 300 B 是 HTTP 响应头（`encodedBodySize` 含头）。本批**单独验过**
   服务端返回的那一份源码里带本批新增的标记（`格子上的 \`src\` 字段就是给这条契约留的探针`），
   且浏览器执行的那一份 URL 的 `v=1790184752` 与 `asset_v('game.js')` 对得上
   ⇒ **"跑的就是改后那一份"成立**，但仍**不是**逐字节比对（与上一批同一条限制）。

## G8. 本批跑了几次 / 几次绿（如实）

| 工具 | 次数 | 结果 |
| --- | --- | --- |
| `pytest tests/`（全量） | 3 次 | 基线 2451 passed（改前）；改后 1 次 3 failed + 2456 passed（两条字段白名单 + 一条契约断言的**预期红**）；补齐后 **2459 passed** |
| `tests/test_replay_lost_priority.py` | 6 次 | 4 次全绿 / 2 次红（都是我自己写错判据：一次是"两次 `ast.parse` 不是同一棵树"、一次是"冲突态没推进记录器"⇒ 守卫恒绿）。**两次都不是产品坏了**，是红证驱动器与守卫自己的问题 |
| 红证（改坏→红→改回） | 9 个 case | 9/9 都红；每个 case 跑完都逐字节还原（脚本自己断言） |
| `tools/dom_replay_frame_check.mjs` | 3 次 | 改前 78 项全绿；改后 **85 项全绿**；红证 2 次都红 2/85 |
| `tools/dom_contract_check.mjs` | 1 次 | 全通过（不受影响） |
| `tools/replay_check.mjs`（真浏览器 E2E） | **2 次** | 第 1 次红：**连续 5 次都没拿到先手**（猜拳输 5 次，理论概率约 3%）⇒ 按"工具脆/环境"处理，原样重跑；第 2 次**全部通过** |
| `tools/spectate_check.mjs` | **1 次** | **全部通过**（156 条 PASS，含不变量 1 的全量扫描 + 自我校准腿）—— 本批改了 `server.py` 的 `平等条约` 分支，观战四条不变量按规矩复跑，**没有假红、不用重试** |
| `node --check static/game.js` | 2 次 | 都 0 |

## G9. ★ 本批**新发现的第 7 处**（同类形状：同一件事两份数据源）—— **本批不修**

上一批在 `test_ship_removal_sites.py` 的 `REMOVAL_SITES` 里给 `轰炸` / `硫磺火焰`
那一行留了一句话：「有的点**不在 attack 表**里 ⇒ 回放可能仍缺一格 —— **本批不修**」。
本批顺着查了，**它是真的**，而且比那句话描述的更严重：

**实测（`.tmp/dbg_bombmark.py`，真调 `apply_magic_effect` 三种情形）**

| 情形 | game_logs | 回放 steps | attack 步 | 回放攻击标记键 |
| --- | --- | --- | --- | --- |
| 纯【轰炸】第 1 行（击沉 1 艘） | **0 条** | 0 | **0** | `[]` |
| 纯【硫磺火焰】第 1 行（击杀 1 艘） | 1 条（magic） | 1 | **0** | `[]` |
| 对照：普通炮击 (1,1) | 1 条（attack） | 1 | 1 | `['1,1']` |

而 `caster.attacks`（**真值表**）里是齐的：
`['0,1','1,1(沉)','2,1','3,1','4,1','5,1']`。

**根因（同一个形状）**："这一格被轰过"这件事有**两份**：
① `caster.attacks`（真值表，逐格写、逐格 emit `attack_result` —— 实战前端就靠它画）；
② `game_logs` 里 `type='attack'` 的那条日志（**普通炮击**由 `_apply_attack_result` 写）。
回放只认 ②（`replayComputeFrame` 第 ③ 段 `step.kind === 'attack'`），
而这两张卡**只写 ①** ⇒ 回放里那些格子**一格标记都没有**。

**玩家可见后果**（**⚠️ 这一条是"按判据推出来的"，不是"在 DOM 里看到的"** ——
我试着用 `.tmp/probe_bomb_dom.mjs` 把真 `game.js` 拉起来直接渲染，卡在搭 DOM 桩上
（`game.js` 顶层就查一堆页面元素，桩不齐直接抛），**没跑通、本批没时间补**；
可靠的是上面那三行**数据**：`frame.marks` 只由 attack 步产生，
而这一局的 attack 步是 0 条 ⇒ 那 6 格必然是"没有标记"，
前端 `replayCellOf` 在没有标记也没有船格时给出的就是"海面：没有船，也没被打过"）：
回放里被轰炸/硫磺火焰轰过的那一行 6 格画成 `· 海面`，
只有"恰好把船打沉的那一格"会以沉没 ✕ 出现（那是 `ships` 时间线兜的，不是标记）。
⇒ 那是一份**残缺的棋盘**，也正是本项目最怕的"看着对、其实少东西"。

**本批为什么不动它**（如实说明，不硬塞进这一批）：

* 它**不属于本批两个口子**（那两个都收口了），且修它必然要碰**玩家可见的游戏日志面板**
  （`add_game_log`）或给回放加新 step 类型 —— 两条都是**契约级**改动，
  与本批"偏内部正确性"的定位冲突；
* 两条候选改法都要先定口径（我倾向后者，但**没做**）：
  a. 在两张卡里逐格调 `add_game_log(..., type='attack', detail=attack_result)` ——
     最小、且 `replay.note` 一行都不用改（它已经能把 `type='attack'` 映射成 attack 步），
     **代价**：游戏日志面板会多出 N 条（玩家可见行为变化，必须自己确认这能不能接受）；
  b. 给回放加一个不写游戏日志的登记口（`replay.note_cells(room, attacker, [(x,y,hit,sunk)])`
     + 新 step 类型 + 前端 `replayComputeFrame` 认它）—— 不动玩家面板，
     **代价**：新增一种 step 类型（前端 + 契约 + 守卫都要跟上），字节也要量。

**判据（留给下一次）**：`replay.build(room)['steps']` 里有没有 attack 步，
与 `caster.attacks` 的格集**必须相等**（"轰过的格子"只有一份真值表）；
本批**没有**为它写任何守卫（不写恒真的假断言）。

⚠️ 顺带更正 §F4 第 5/6 行那句"回放靠攻击标记本来就画得出红叉"：
**画不出**（本批实测）—— 那两处的 `kind` 仍是 `attack`（分类没错），
但那句话描述的**后果**是错的，`REMOVAL_SITES` 的 `why` 里也留了同款措辞。
本批**没有**去改那张表（改它要连带改 `tests/test_ship_removal_sites.py` 的断言），
只在已 `docs/` 里更正。


